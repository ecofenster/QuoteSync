import{randomUUID}from'node:crypto';
const json=value=>{try{return JSON.parse(value)}catch{return value}};
export async function readCalculatorAdminConfiguration(db){const[catalogue,rules,packages]=await Promise.all([db.all('SELECT * FROM project_calculator_admin_catalogue_items ORDER BY category,label'),db.all('SELECT * FROM project_calculator_admin_rules ORDER BY rule_key'),db.all('SELECT * FROM project_calculator_admin_package_rules ORDER BY package_code')]);return{catalogue:catalogue.map(row=>({id:row.id,category:row.category,label:row.label,rateType:row.rate_type,priceAmount:row.price_amount,currency:row.currency,variant:json(row.variant_json),supplier:row.supplier,notes:row.notes,active:!!row.active,version:row.version})),rules:Object.fromEntries(rules.map(row=>[row.rule_key,{value:json(row.rule_value_json),version:row.version}])),packageRules:Object.fromEntries(packages.map(row=>[row.package_code,{inclusions:json(row.inclusions_json),version:row.version}]))};}
export async function snapshotCalculatorAdminConfiguration(db,scenarioId,revision,now=new Date().toISOString()){const config=await readCalculatorAdminConfiguration(db);await db.run('INSERT INTO project_calculator_lab_catalogue_snapshots(id,scenario_id,scenario_revision,catalogue_json,rules_json,package_rules_json,created_at) VALUES(?,?,?,?,?,?,?)',randomUUID(),scenarioId,revision,JSON.stringify(config.catalogue),JSON.stringify(config.rules),JSON.stringify(config.packageRules),now);return config;}
export function createCalculatorAdminService(db){return{getConfiguration:()=>readCalculatorAdminConfiguration(db),async updateCatalogueItem(id,input){const existing=await db.get('SELECT * FROM project_calculator_admin_catalogue_items WHERE id=?',id);if(!existing)return null;if(input.priceAmount!=null&&input.priceAmount!==''&&!/^\d+(?:\.\d+)?$/.test(String(input.priceAmount)))throw Object.assign(new Error('Catalogue price must be a non-negative decimal string or blank.'),{code:'invalid_catalogue'});await db.run('UPDATE project_calculator_admin_catalogue_items SET label=COALESCE(?,label),price_amount=?,supplier=COALESCE(?,supplier),notes=COALESCE(?,notes),variant_json=COALESCE(?,variant_json),active=COALESCE(?,active),version=version+1,updated_at=? WHERE id=?',input.label??null,input.priceAmount===''?null:input.priceAmount??existing.price_amount,input.supplier??null,input.notes??null,input.variant?JSON.stringify(input.variant):null,typeof input.active==='boolean'?(input.active?1:0):null,new Date().toISOString(),id);return readCalculatorAdminConfiguration(db);},async updateRule(key,value){const result=await db.run('UPDATE project_calculator_admin_rules SET rule_value_json=?,version=version+1,updated_at=? WHERE rule_key=?',JSON.stringify(value),new Date().toISOString(),key);return result.changes?readCalculatorAdminConfiguration(db):null;},async updatePackage(code,inclusions){const result=await db.run('UPDATE project_calculator_admin_package_rules SET inclusions_json=?,version=version+1,updated_at=? WHERE package_code=?',JSON.stringify(inclusions),new Date().toISOString(),code);return result.changes?readCalculatorAdminConfiguration(db):null;}};}
