import { ROUTE_INTEGRATION_CAPABILITIES } from "../projectCalculatorLab/integrations/routeIntegration";

export default function AdminIntegrationsPanel(){
 const googleConfigured=Boolean(String(import.meta.env.VITE_GOOGLE_MAPS_API_KEY||"").trim());
 const w3wConfigured=Boolean(String(import.meta.env.VITE_WHAT3WORDS_API_KEY||"").trim());
 return <div className="admin-card ui-card" style={{padding:20,display:"grid",gap:12}}><div className="admin-section-title">Integrations</div><p>Central capability status. API keys remain environment configuration and are never entered in Project Calculator.</p><div>Google Maps: <strong>{googleConfigured?"configured in frontend environment":"not configured"}</strong></div><div>what3words: <strong>{w3wConfigured?"configured in frontend environment":"not configured"}</strong></div><ul><li>Map display and geocoding: supported by the existing Google integration.</li><li>Routable distance and normal duration: {ROUTE_INTEGRATION_CAPABILITIES.googleRoutableDistance?"supported through the existing Distance Matrix provider":"unavailable"}.</li><li>Traffic-aware duration: unavailable in the current provider.</li><li>what3words to coordinates: supported when the configured plan permits conversion.</li></ul><p>Runtime API enablement and billing permissions cannot be proven from source configuration; a failed calculator route reports the capability gap without storing a misleading snapshot.</p></div>;
}
