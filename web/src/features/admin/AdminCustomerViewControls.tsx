import { useEffect,useState } from "react";
import Toggle from "../../components/Toggle";
import { apiFetch } from "../../services/api/apiClient";
import { DEFAULT_CUSTOMER_VIEW_POLICY,type CustomerViewPolicy } from "../estimateCommercial/customerViewPolicy";
const KEY="estimate.customerViewPolicy";
const LABELS:Record<keyof CustomerViewPolicy,string>={room:"Room",dimensions:"Dimensions",itemPrice:"Item Price",quantityPrice:"Quantity Price",alternative:"Alternative",quickConfigurator:"Quick Configurator editing",duplicate:"Duplicate",reorder:"Position reordering",manufacturerImport:"Import Manufacturer Quote",customerQuotation:"Customer Quotation action"};
export default function AdminCustomerViewControls(){const[policy,setPolicy]=useState(DEFAULT_CUSTOMER_VIEW_POLICY),[exists,setExists]=useState(false),[status,setStatus]=useState("");useEffect(()=>{void apiFetch("/api/settings/projectPreferences").then(rows=>{const row=(Array.isArray(rows)?rows:[]).find((item:any)=>item.key===KEY);if(row){setPolicy({...DEFAULT_CUSTOMER_VIEW_POLICY,...row.value});setExists(true)}})},[]);async function save(){await apiFetch(exists?`/api/settings/${KEY}`:"/api/settings",{method:exists?"PUT":"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(exists?{value:policy,group_name:"projectPreferences"}:{key:KEY,value:policy,group_name:"projectPreferences"})});setExists(true);setStatus("Customer View controls saved. Internal commercial fields remain unavailable.")}return <section className="admin-card admin-card--section ui-card"><div className="admin-group-title">Estimate Customer View</div><div className="admin-body-copy">Controls optional customer-safe workspace capabilities. Supplier costs, FX, markups and margins are never available here.</div><div className="admin-customer-view-controls">{(Object.keys(LABELS) as Array<keyof CustomerViewPolicy>).map(key=><label key={key}><span>{LABELS[key]}</span><Toggle ariaLabel={`${LABELS[key]} in Customer View`} value={policy[key]} onChange={value=>setPolicy(current=>({...current,[key]:value}))}/></label>)}</div><div className="admin-form-actions"><button className="ui-button ui-button--primary" onClick={()=>void save()}>Save Customer View Controls</button>{status?<span role="status">{status}</span>:null}</div></section>}
