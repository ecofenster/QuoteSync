import { useEffect, useMemo, useState } from "react";
import type { Client } from "../../models/types";
import { documentRecordsApi, type CanonicalDocumentRecord } from "../../services/documents/documentRecordsApi";
import { buildCustomerSafePortalProjection, type CustomerSafeCommercial } from "./customerSafePortalProjection";
import "./clientPortalPreview.css";

type PortalTab="dashboard"|"estimates"|"orders"|"rejected"|"documents";
const tabs:Array<{id:PortalTab;label:string}>=[{id:"dashboard",label:"Dashboard"},{id:"estimates",label:"Estimates"},{id:"orders",label:"Orders"},{id:"rejected",label:"Rejected"},{id:"documents",label:"Documents"}];
const money=(value:number|null,currency:string)=>value==null?"Not available":new Intl.NumberFormat("en-GB",{style:"currency",currency}).format(value);

export default function ClientPortalPreview({client,commercialByEstimateId={},releasedEstimateIds=[],orderEstimateIds=[],rejectedEstimateIds=[],releasedDocumentIds=[]}:{client:Client;commercialByEstimateId?:Record<string,Partial<CustomerSafeCommercial>>;releasedEstimateIds?:string[];orderEstimateIds?:string[];rejectedEstimateIds?:string[];releasedDocumentIds?:string[]}){
  const [tab,setTab]=useState<PortalTab>("dashboard"),[documents,setDocuments]=useState<CanonicalDocumentRecord[]>([]),[error,setError]=useState("");
  useEffect(()=>{let active=true;documentRecordsApi.listClient(String(client.id)).then((result)=>{if(active)setDocuments(result.documents)}).catch((reason)=>{if(active)setError(reason instanceof Error?reason.message:"Customer-safe documents could not be loaded.")});return()=>{active=false}},[client.id]);
  const projection=useMemo(()=>buildCustomerSafePortalProjection({client,documents,commercialByEstimateId,releasedEstimateIds,orderEstimateIds,rejectedEstimateIds,releasedDocumentIds}),[client,commercialByEstimateId,documents,releasedEstimateIds,orderEstimateIds,rejectedEstimateIds,releasedDocumentIds]);
  const estimateList=(items:typeof projection.estimates,empty:string)=><div className="client-portal-preview__list">{items.length?items.map((estimate)=><article className="ui-card" key={estimate.id}><div><strong>{estimate.estimateRef}</strong><small>Revision {estimate.revisionNo} · {estimate.status.replaceAll("_"," ")}</small></div><strong>{money(estimate.commercial.total,estimate.commercial.currency)}</strong><button type="button" className="ui-button ui-button--ghost" disabled>Review Estimate</button></article>):<div className="ui-empty-state">{empty}</div>}</div>;
  return <section className="client-portal-preview" aria-labelledby="portal-preview-title"><header className="client-portal-preview__notice ui-status ui-status--warning"><strong>Internal portal preview</strong><span>This customer-safe projection is not a public route. Authentication, invitation lifecycle, session expiry and document authorization must be approved before external access.</span></header><div className="client-portal-preview__shell"><header><div><span className="ui-eyebrow">Customer view · {projection.client.reference}</span><h2 id="portal-preview-title">{projection.client.projectName||projection.client.displayName}</h2></div><span className="ui-status ui-status--muted">{projection.currentStatus}</span></header><nav className="client-portal-preview__tabs" aria-label="Portal areas">{tabs.map((item)=><button key={item.id} type="button" className={tab===item.id?"ui-button ui-button--primary":"ui-button ui-button--ghost"} aria-current={tab===item.id?"page":undefined} onClick={()=>setTab(item.id)}>{item.label}</button>)}</nav>{error?<div className="ui-status ui-status--error" role="alert">{error}</div>:null}{tab==="dashboard"?<div className="client-portal-preview__dashboard"><section className="client-portal-preview__hero ui-card"><span>Current next step</span><h3>{projection.nextAction||"Your project is being prepared"}</h3><p>The latest issued information remains prominent here. Internal supplier evidence and commercial diagnostics are excluded by contract.</p></section>{projection.latestEstimate?<section className="client-portal-preview__latest"><div><span className="ui-eyebrow">Latest Estimate</span><h3>{projection.latestEstimate.estimateRef} · Revision {projection.latestEstimate.revisionNo}</h3></div><div className="client-portal-preview__costs">{[["Supply Only",projection.latestEstimate.commercial.supplyOnly],["Installation",projection.latestEstimate.commercial.installation],["VAT",projection.latestEstimate.commercial.vat],["Total",projection.latestEstimate.commercial.total]].map(([label,value])=><article className="ui-card" key={String(label)}><span>{label}</span><strong>{money(value as number|null,projection.latestEstimate!.commercial.currency)}</strong></article>)}</div><div className="ui-action-row"><button className="ui-button ui-button--primary" type="button" disabled>Review Estimate</button><button className="ui-button ui-button--ghost" type="button" disabled>Raise Order</button><button className="ui-button ui-button--danger" type="button" disabled>Decline Estimate</button></div></section>:<div className="ui-empty-state">No customer Estimate has been issued yet.</div>}</div>:null}{tab==="estimates"?estimateList(projection.estimates,"No Estimates are available."):null}{tab==="orders"?estimateList(projection.orders,"No accepted Orders yet."):null}{tab==="rejected"?estimateList(projection.rejected,"No rejected Estimates."):null}{tab==="documents"?<div className="client-portal-preview__list">{projection.documents.length?projection.documents.map((document)=><article className="ui-card" key={document.id}><div><strong>{document.fileName}</strong><small>{document.documentType.replaceAll("_"," ")} · {document.revision}</small></div><span>{new Date(document.modifiedAt).toLocaleDateString("en-GB")}</span>{document.openUrl?<a className="ui-button ui-button--ghost" href={document.openUrl}>Open</a>:<span className="ui-status ui-status--muted">Evidence retained</span>}</article>):<div className="ui-empty-state">No approved project documents are available.</div>}</div>:null}</div></section>;
}
