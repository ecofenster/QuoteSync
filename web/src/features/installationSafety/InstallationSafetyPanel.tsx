import {useEffect,useMemo,useState} from "react";
import {apiFetch} from "../../services/api/apiClient";
import {installationSafetyApi,type RamsHazard,type RamsRecord} from "./installationSafetyApi";
import "./installationSafety.css";

const idempotency=()=>crypto.randomUUID(),lines=(value:string)=>value.split(/\r?\n/).map(item=>item.trim()).filter(Boolean);
type DocumentRecord={canonicalRecordId?:string;id:string;fileName:string;documentType:string;revision?:string;openUrl?:string|null};
export default function InstallationSafetyPanel({estimateId,recordKind}:{estimateId:string;recordKind:"Estimate"|"Order"}){
  const[open,setOpen]=useState(false),[record,setRecord]=useState<RamsRecord|null>(null),[documents,setDocuments]=useState<DocumentRecord[]>([]),[step,setStep]=useState<"work"|"hazards"|"review">("work"),[busy,setBusy]=useState(false),[message,setMessage]=useState("");
  const[draft,setDraft]=useState({title:"",workSummary:"",siteDescription:"",methodSteps:"",emergencyArrangements:"",publicProtection:"",wasteArrangements:"",specialistAssessment:"",reviewerName:"",reviewerRole:"",competentPersonConfirmed:false,supportingDocumentIds:[] as string[],hazards:[] as RamsHazard[],attendanceStart:"",attendanceEnd:"",requiredQualificationTypes:[] as string[]});
  const hydrate=(value:RamsRecord)=>{setRecord(value);const source=value.draft||value.issued;if(source)setDraft({title:source.title,workSummary:source.workSummary,siteDescription:source.siteConditions?.description||"",methodSteps:source.methodSteps.join("\n"),emergencyArrangements:source.emergencyArrangements,publicProtection:source.publicProtection,wasteArrangements:source.wasteArrangements,specialistAssessment:source.specialistAssessment,reviewerName:source.reviewerName||"",reviewerRole:source.reviewerRole||"",competentPersonConfirmed:source.competentPersonConfirmed,supportingDocumentIds:source.supportingDocumentIds,hazards:source.hazards,attendanceStart:value.context.attendanceStart||"",attendanceEnd:value.context.attendanceStart||"",requiredQualificationTypes:[]})};
  useEffect(()=>{if(!open)return;void Promise.all([installationSafetyApi.list(estimateId),apiFetch(`/api/documents?estimate_id=${encodeURIComponent(estimateId)}`) as Promise<{documents:DocumentRecord[]}>]).then(([records,files])=>{if(records[0])hydrate(records[0]);setDocuments(files.documents||[])}).catch(error=>setMessage(error instanceof Error?error.message:"RAMS information could not be loaded."))},[open,estimateId]);
  const payload=()=>({title:draft.title,workSummary:draft.workSummary,siteConditions:{description:draft.siteDescription},hazards:draft.hazards,methodSteps:lines(draft.methodSteps),emergencyArrangements:draft.emergencyArrangements,publicProtection:draft.publicProtection,wasteArrangements:draft.wasteArrangements,specialistAssessment:draft.specialistAssessment,supportingDocumentIds:draft.supportingDocumentIds,reviewerName:draft.reviewerName,reviewerRole:draft.reviewerRole,competentPersonConfirmed:draft.competentPersonConfirmed,attendanceStart:draft.attendanceStart,attendanceEnd:draft.attendanceEnd,requiredQualificationTypes:draft.requiredQualificationTypes});
  const act=async(progress:string,action:()=>Promise<RamsRecord>,success:string)=>{setBusy(true);setMessage(progress);try{const value=await action();hydrate(value);setMessage(success);return true}catch(error){setMessage(`${error instanceof Error?error.message:"RAMS could not be saved."} Your entries and any completed revision remain available; review the missing information and retry.`);return false}finally{setBusy(false)}};
  const ensure=async()=>{if(record)return record;setBusy(true);setMessage("Preparing a RAMS draft…");try{const value=await installationSafetyApi.create(estimateId);hydrate(value);setMessage("RAMS draft prepared from this exact schedule. Next: review Project and work details.");return value}catch(error){setMessage(error instanceof Error?error.message:"A RAMS draft could not be prepared.");return null}finally{setBusy(false)}};
  const relevantDocuments=useMemo(()=>documents.filter(item=>item.canonicalRecordId),[documents]);
  return <section className="installation-safety"><button type="button" className="ui-button" onClick={async()=>{setOpen(current=>!current);if(!open)await ensure()}}>Create / review RAMS</button>{open?<div className="ui-card installation-safety__workspace"><header><div><span className="ui-eyebrow">{recordKind} installation</span><h3>Risk assessment and method statement</h3><p>Project and work → Site hazards and controls → Review → Issue PDF</p></div><button type="button" className="ui-button" onClick={()=>setOpen(false)}>Close</button></header>{message?<p className="ui-status" role={/could not|cannot|missing|retry/i.test(message)?"alert":"status"}>{message}</p>:null}{record?.reviewReasons.map(reason=><p className="ui-status ui-status--warning" key={reason}>{reason}</p>)}
    <nav className="ui-tabs" aria-label="RAMS steps">{([['work','1. Project and work'],['hazards','2. Hazards and controls'],['review','3. Review and issue']] as const).map(([value,label])=><button type="button" className="ui-tab" aria-selected={step===value} key={value} onClick={()=>setStep(value)}>{label}</button>)}</nav>
    {record&&record.draft&&step==="work"?<div className="installation-safety__step"><div className="installation-safety__summary"><strong>{record.context.orderReference||record.context.estimateReference}</strong><span>{record.context.clientName} · {record.context.projectName||record.context.siteAddress}</span><span>{record.context.positions.length} scheduled position(s) · Team: {record.context.teamName||"Not confirmed"}</span></div><label>Document title<input className="ui-input" value={draft.title} onChange={event=>setDraft(current=>({...current,title:event.currentTarget.value}))}/></label><label>Actual work<textarea className="ui-textarea" rows={4} value={draft.workSummary} onChange={event=>setDraft(current=>({...current,workSummary:event.currentTarget.value}))}/></label><label>Site conditions<textarea className="ui-textarea" rows={4} value={draft.siteDescription} onChange={event=>setDraft(current=>({...current,siteDescription:event.currentTarget.value}))} placeholder="Access, occupants/public, storage, ground conditions and known constraints"/></label><label>Method, one step per line<textarea className="ui-textarea" rows={6} value={draft.methodSteps} onChange={event=>setDraft(current=>({...current,methodSteps:event.currentTarget.value}))}/></label><details><summary>Select retained site photos and supporting files</summary><p>Upload new evidence through Files first. Selecting it here does not copy or overwrite the original.</p>{relevantDocuments.map(file=>{const id=file.canonicalRecordId!;return <label className="installation-safety__file" key={id}><input type="checkbox" checked={draft.supportingDocumentIds.includes(id)} onChange={event=>setDraft(current=>({...current,supportingDocumentIds:event.currentTarget.checked?[...current.supportingDocumentIds,id]:current.supportingDocumentIds.filter(value=>value!==id)}))}/><span>{file.fileName}<small>{file.documentType} · {file.revision||"Current"}</small></span></label>})}{!relevantDocuments.length?<p>No retained provider files are available for this Estimate. Open Files to upload or link evidence.</p>:null}</details><button type="button" className="ui-button ui-button--primary" disabled={busy} onClick={async()=>{if(await act("Saving Project and work…",()=>installationSafetyApi.save(record.id,payload()),"Project and work saved. Next: review each relevant hazard and its controls."))setStep("hazards")}}>{busy?"Saving…":"Save and review hazards"}</button></div>:null}
    {record&&record.draft&&step==="hazards"?<div className="installation-safety__step">{draft.hazards.map((item,index)=><article className="ui-card installation-safety__hazard" key={`${item.hazard}-${index}`}><div><strong>{item.hazard}</strong><select className="ui-input" aria-label={`${item.hazard} applies`} value={item.applies===null?"":item.applies?"yes":"no"} onChange={event=>setDraft(current=>({...current,hazards:current.hazards.map((hazard,i)=>i===index?{...hazard,applies:event.currentTarget.value===""?null:event.currentTarget.value==="yes"}:hazard)}))}><option value="">Not reviewed</option><option value="yes">Relevant to this work</option><option value="no">Not relevant</option></select></div>{item.applies!==false?<label>Controls, one per line<textarea className="ui-textarea" rows={3} value={item.controls.join("\n")} onChange={event=>setDraft(current=>({...current,hazards:current.hazards.map((hazard,i)=>i===index?{...hazard,controls:lines(event.currentTarget.value)}:hazard)}))}/></label>:null}<label className="installation-safety__check"><input type="checkbox" checked={item.reviewed} onChange={event=>setDraft(current=>({...current,hazards:current.hazards.map((hazard,i)=>i===index?{...hazard,reviewed:event.currentTarget.checked}:hazard)}))}/> Reviewed for this Project</label></article>)}<label>Specialist assessment or external documents required<textarea className="ui-textarea" rows={3} value={draft.specialistAssessment} onChange={event=>setDraft(current=>({...current,specialistAssessment:event.currentTarget.value}))} placeholder="Record when the basic template is not enough"/></label><button type="button" className="ui-button ui-button--primary" disabled={busy} onClick={async()=>{if(await act("Saving hazards and controls…",()=>installationSafetyApi.save(record.id,payload()),"Hazards and controls saved. Next: complete the responsible-person review."))setStep("review")}}>{busy?"Saving…":"Save and review document"}</button></div>:null}
    {record&&record.draft&&step==="review"?<div className="installation-safety__step"><p>Generated prompts remain a draft until a competent responsible person reviews the actual work. This RAMS is separate from a Construction Phase Plan and specialist plans.</p><label>Public protection<textarea className="ui-textarea" rows={3} value={draft.publicProtection} onChange={event=>setDraft(current=>({...current,publicProtection:event.currentTarget.value}))}/></label><label>Waste arrangements<textarea className="ui-textarea" rows={3} value={draft.wasteArrangements} onChange={event=>setDraft(current=>({...current,wasteArrangements:event.currentTarget.value}))}/></label><label>Emergency arrangements<textarea className="ui-textarea" rows={3} value={draft.emergencyArrangements} onChange={event=>setDraft(current=>({...current,emergencyArrangements:event.currentTarget.value}))}/></label><div className="installation-safety__review-grid"><label>Reviewer name<input className="ui-input" value={draft.reviewerName} onChange={event=>setDraft(current=>({...current,reviewerName:event.currentTarget.value}))}/></label><label>Role / competence basis<input className="ui-input" value={draft.reviewerRole} onChange={event=>setDraft(current=>({...current,reviewerRole:event.currentTarget.value}))}/></label><label>Attendance start<input className="ui-input" type="date" value={draft.attendanceStart} onChange={event=>setDraft(current=>({...current,attendanceStart:event.currentTarget.value}))}/></label><label>Attendance end<input className="ui-input" type="date" value={draft.attendanceEnd} onChange={event=>setDraft(current=>({...current,attendanceEnd:event.currentTarget.value}))}/></label></div><label className="installation-safety__check"><input type="checkbox" checked={draft.competentPersonConfirmed} onChange={event=>setDraft(current=>({...current,competentPersonConfirmed:event.currentTarget.checked}))}/> I am competent and have reviewed this project-specific RAMS.</label><div className="ui-action-row"><button type="button" className="ui-button" disabled={busy} onClick={()=>void act("Saving reviewed draft…",()=>installationSafetyApi.save(record.id,payload()),"Draft saved. Nothing has been issued.")}>{busy?"Saving…":"Save draft"}</button><button type="button" className="ui-button ui-button--primary" disabled={busy} onClick={()=>void act("Issuing RAMS PDF…",()=>installationSafetyApi.issue(record.id,payload()),"RAMS issued. The PDF and source revision are preserved. Next: brief the attending workforce and select the PDF in the reviewed installer pack.")}>{busy?"Issuing RAMS…":"Issue RAMS PDF"}</button></div></div>:null}
    {record?.issued?<section className="installation-safety__issued"><strong>Current issued version: R{record.issued.version}</strong><span>{record.issued.pdfFileName}</span><a className="ui-button" href={record.issued.downloadUrl||"#"} target="_blank" rel="noreferrer">Open issued PDF</a>{!record.draft?<button type="button" className="ui-button" disabled={busy} onClick={()=>void act("Creating a reviewed revision…",()=>installationSafetyApi.revise(record.id),"New editable RAMS revision created. The issued version remains unchanged.")}>Create new revision</button>:null}</section>:null}
  </div>:null}</section>;
}
