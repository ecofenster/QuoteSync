import assert from "node:assert/strict";
import { spawn } from "node:child_process";
import { createServer } from "node:http";
import { createServer as createNetServer } from "node:net";
import { existsSync } from "node:fs";
import { mkdtemp, rm, writeFile, readFile } from "node:fs/promises";
import os from "node:os";
import path from "node:path";
import express from "express";
import sqlite3 from "sqlite3";
import { open } from "sqlite";
import { build } from "esbuild";
import { initializeWorkflowSchema } from "../server/features/workflow/workflowSchema.js";
import { initializeLifecycleSchema } from "../server/features/lifecycle/lifecycleSchema.js";
import { createLifecycleRouter } from "../server/routes/lifecycle.js";
import { createCommunicationsRouter } from "../server/routes/communications.js";
import { createCommunicationRepository } from "../server/features/communications/communicationRepository.js";
import { createBrowserRunController, countBrowserRunProfiles } from "./browser-run-lifecycle.mjs";

const delay=ms=>new Promise(resolve=>setTimeout(resolve,ms)),freePort=async()=>{const server=createNetServer();await new Promise((resolve,reject)=>{server.once("error",reject);server.listen(0,"127.0.0.1",resolve)});const port=server.address().port;await new Promise(resolve=>server.close(resolve));return port};
const waitFor=async(fn,message,timeout=30000)=>{const started=Date.now();while(Date.now()-started<timeout){const value=await fn().catch(()=>false);if(value)return value;await delay(100)}throw new Error(message)};
const chromePath=["C:\\Program Files\\Google\\Chrome\\Application\\chrome.exe","C:\\Program Files (x86)\\Google\\Chrome\\Application\\chrome.exe",`${process.env.LOCALAPPDATA||""}\\Google\\Chrome\\Application\\chrome.exe`].find(existsSync);if(!chromePath)throw new Error("Chrome was not found.");
const controller=createBrowserRunController({throwOnLeak:true,processOptions:{platformName:process.platform}});controller.installInterruptHandlers();let root,db,http,socket;

async function run(){
  const userRuntimeBefore=await fetch("http://127.0.0.1:3001/api/health").then(response=>response.ok?response.json():null).catch(()=>null),port=await freePort(),debugPort=await freePort(),base=`http://127.0.0.1:${port}`;
  root=await mkdtemp(path.join(os.tmpdir(),"qs-rfq-browser-"));db=await open({filename:path.join(root,"fixture.db"),driver:sqlite3.Database});await db.exec(`PRAGMA foreign_keys=ON;
    CREATE TABLE clients(id TEXT PRIMARY KEY,name TEXT,client_ref TEXT,deleted_at TEXT);
    CREATE TABLE projects(id TEXT PRIMARY KEY,client_id TEXT,name TEXT,deleted_at TEXT);
    CREATE TABLE estimates(id TEXT PRIMARY KEY,project_id TEXT,estimate_ref TEXT,revision_no INTEGER,status TEXT,updated_at TEXT,deleted_at TEXT);
    CREATE TABLE followups(id TEXT PRIMARY KEY,origin_event_id TEXT);
    CREATE TABLE supplier_commercial_defaults(supplier_code TEXT PRIMARY KEY,supplier_name TEXT,policy_json TEXT DEFAULT '{}',pricing_display_policy_json TEXT DEFAULT '{}',updated_at TEXT,active INTEGER DEFAULT 1);
    CREATE TABLE canonical_documents(id TEXT PRIMARY KEY,project_id TEXT,file_name TEXT,mime_type TEXT,size_bytes INTEGER,document_type TEXT,provider_file_id TEXT,provider_revision TEXT,checksum TEXT,folder_path TEXT,provider_modified_at TEXT,removed_at TEXT,trashed INTEGER DEFAULT 0);
    INSERT INTO clients VALUES('client-1','Disposable Client','TEST-CL-1',NULL);
    INSERT INTO projects VALUES('project-1','client-1','Disposable Project',NULL);
    INSERT INTO estimates VALUES('estimate-1','project-1','TEST-EST-1',1,'Draft','2026-09-11T10:00:00.000Z',NULL);
    INSERT INTO supplier_commercial_defaults(supplier_code,supplier_name,updated_at) VALUES('ZYLE','Zyle Fenster','2026-09-11T10:00:00.000Z');
    INSERT INTO canonical_documents VALUES('document-1','project-1','Approved drawing.pdf','application/pdf',120,'project_drawing','drive-file-1','7','abc123','2026/TEST/Drawings','2026-09-11T10:00:00.000Z',NULL,0);`);await initializeWorkflowSchema(db);await initializeLifecycleSchema(db);
  await build({entryPoints:[path.resolve("tests/fixtures/SupplierRfqAcceptance.tsx")],bundle:true,format:"esm",platform:"browser",jsx:"automatic",outdir:root,define:{"import.meta.env.VITE_API_BASE_URL":JSON.stringify(base),"import.meta.env.DEV":"false","import.meta.env.PROD":"true"}});
  let postCount=0,failFirst=true;const app=express();app.use(express.json());app.use((req,res,next)=>{if(req.method==="POST"&&req.path.endsWith("/supplier-enquiries")){postCount+=1;if(failFirst){failFirst=false;return res.status(503).json({error:"Temporary test interruption. Your reviewed details have not been discarded.",code:"disposable_failure"})}return delay(220).then(next)}return next()});
  const repository=createCommunicationRepository(db);
  await db.exec(`CREATE TABLE portal_review_submissions(id TEXT PRIMARY KEY); CREATE TABLE estimate_revision_releases(id TEXT PRIMARY KEY);
    INSERT INTO portal_review_submissions VALUES('history-review'); INSERT INTO estimate_revision_releases VALUES('history-release');
    INSERT INTO supplier_revision_requests(id,review_submission_id,source_release_id,successor_estimate_id,subject,summary_json,created_by,created_at,updated_at) VALUES('history-request','history-review','history-release','estimate-1','Disposable history','{}','test-staff','2026-09-12','2026-09-12');`);
  for(let index=0;index<12;index++)await db.run('INSERT INTO supplier_revision_review_history(id,request_id,source_identity,checks_json,reviewed_by,reviewed_at) VALUES(?,?,?,?,?,?)',`history-${String(index).padStart(2,'0')}`,'history-request','test-source',JSON.stringify([{field_key:'finish',before_value:'White',expected_value:'Green',after_value:'Green',after_source_reference:'Supplier page 2',status:'implemented',resolved_by:'test-staff',resolved_at:'2026-09-12T10:00:00Z'}]),'test-staff','2026-09-12T10:00:00Z');
  app.put('/api/lifecycle/supplier-revisions/history-request/verification',(_req,res)=>res.status(503).json({error:'Temporary review interruption. Your entered checks are retained; try again.'}));
  await db.exec("ALTER TABLE projects ADD COLUMN context_year INTEGER DEFAULT 2026; ALTER TABLE estimates ADD COLUMN created_at TEXT;");
  let storedFiles=0;
  const supplierBytes=Buffer.from("Disposable retained supplier estimate bytes"),folderPath="Disposable Project/Estimates/TEST-EST-1/Suppliers/Zyle Fenster";
  const drive={
    reviewCommunicationSupplierDocument:async input=>({status:"reviewed",destinationExists:true,folderPath,...input,supplierName:"Zyle Fenster",conflict:{kind:storedFiles?"already_filed":"new_file",message:storedFiles?"This attachment was already saved.":"No matching file exists.",evidence:"Disposable provider adapter",existingFile:null,recommendedDecision:storedFiles?"reuse_existing":"save",decisions:[storedFiles?"reuse_existing":"save"]}}),
    storeCommunicationSupplierDocument:async input=>{
      assert.equal(input.providerMessageId,"reply-old");assert.equal(input.estimateId,"estimate-1");assert.deepEqual(Buffer.from(input.bytes),supplierBytes);
      const duplicate=storedFiles>0;
      if(!duplicate){await writeFile(path.join(root,"supplier-estimate.docx"),input.bytes);storedFiles+=1;await db.run("INSERT INTO canonical_documents(id,project_id,file_name,mime_type,size_bytes,document_type,provider_file_id,folder_path) VALUES('filed-reply','project-1',?,'application/vnd.openxmlformats-officedocument.wordprocessingml.document',?,'supplier_quotation','test-provider-file',?)",input.fileName,input.bytes.length,folderPath);}
      return {status:"stored",duplicate,documentId:"filed-reply",providerFileId:"test-provider-file",fileName:input.fileName,folderPath,webViewLink:null};
    }
  };
  app.use("/api/communications",createCommunicationsRouter({databasePromise:Promise.resolve(db),serviceOptions:{drive,gmailOptions:{pageSize:1},workspace:{status:async()=>({connected:true,capabilities:{gmail:{available:true}}})},gmail:{readMessage:async id=>repository.findByProviderId("google_workspace",id),attachment:async(id)=>{assert.equal(id,"reply-old");return supplierBytes;}}}}));
  app.use("/api/lifecycle",createLifecycleRouter({databasePromise:Promise.resolve(db),serviceOptions:{environment:{NODE_ENV:"development",QUOTESUITE_TEST_JOURNEY:"1",QUOTESUITE_TEST_FACTORY_EMAIL:"factory@example.test"}}}));app.get("/SupplierRfqAcceptance.js",(_req,res)=>res.sendFile(path.join(root,"SupplierRfqAcceptance.js")));app.get("/SupplierRfqAcceptance.css",(_req,res)=>res.sendFile(path.join(root,"SupplierRfqAcceptance.css")));app.use((_req,res)=>res.type("html").send('<!doctype html><html><head><link rel="stylesheet" href="/SupplierRfqAcceptance.css"></head><body><div id="root"></div><script type="module" src="/SupplierRfqAcceptance.js"></script></body></html>'));http=createServer(app);await new Promise(resolve=>http.listen(port,"127.0.0.1",resolve));
  const profile=await controller.createProfile({label:"supplier-rfq",debugPort}),browser=spawn(chromePath,["--headless=new",`--remote-debugging-port=${debugPort}`,`--user-data-dir=${profile}`,"--no-first-run","--disable-gpu","--disable-extensions",base],{stdio:"ignore",windowsHide:true});controller.setRun({child:browser,userDataDir:profile,debugPort,profileProcessCountDuring:await countBrowserRunProfiles(profile,{platformName:process.platform})});await waitFor(()=>fetch(`http://127.0.0.1:${debugPort}/json/version`).then(response=>response.ok),"Owned Chrome did not start");const targets=await(await fetch(`http://127.0.0.1:${debugPort}/json/list`)).json(),target=targets.find(item=>item.type==="page"&&item.url.startsWith(base));socket=new WebSocket(target.webSocketDebuggerUrl);await new Promise((resolve,reject)=>{socket.addEventListener("open",resolve,{once:true});socket.addEventListener("error",reject,{once:true})});let id=0;const pending=new Map(),send=(method,params={})=>new Promise((resolve,reject)=>{const call=++id;pending.set(call,{resolve,reject});socket.send(JSON.stringify({id:call,method,params}))}),evaluate=async expression=>(await send("Runtime.evaluate",{expression,awaitPromise:true,returnByValue:true})).result?.value;socket.addEventListener("message",event=>{const message=JSON.parse(String(event.data));if(message.id&&pending.has(message.id)){const task=pending.get(message.id);pending.delete(message.id);message.error?task.reject(new Error(message.error.message)):task.resolve(message.result)}});await send("Runtime.enable");
  await evaluate("[...document.querySelectorAll('button')].find(item=>item.textContent.trim()==='Request supplier estimate / revision')?.click()");try{await waitFor(()=>evaluate("document.body.innerText.includes('Disposable Client')&&document.body.innerText.includes('Approved drawing.pdf')&&document.body.innerText.includes('Preview only')"),"Supplier request context did not render")}catch(error){throw new Error(`${error.message}: ${JSON.stringify(await evaluate("({text:document.body.innerText,html:document.body.innerHTML.slice(0,5000)})"))}`)}await evaluate("(()=>{const setInput=Object.getOwnPropertyDescriptor(HTMLInputElement.prototype,'value').set,setSelect=Object.getOwnPropertyDescriptor(HTMLSelectElement.prototype,'value').set;const supplier=[...document.querySelectorAll('select')].find(item=>item.querySelector('option[value=ZYLE]'));setSelect.call(supplier,'ZYLE');supplier.dispatchEvent(new Event('change',{bubbles:true}));const email=document.querySelector('input[type=email]');setInput.call(email,'factory@example.test');email.dispatchEvent(new Event('input',{bubbles:true}));email.dispatchEvent(new Event('change',{bubbles:true}));return true})()");
  await evaluate("[...document.querySelectorAll('button')].find(item=>item.textContent.trim()==='Prepare for review')?.click()");await waitFor(()=>evaluate("document.body.innerText.includes('Temporary test interruption')"),"Recoverable supplier-request error was not shown");const preserved=await evaluate("({email:document.querySelector('input[type=email]')?.value,selected:document.querySelector('.supplier-rfq__documents input')?.checked,subject:[...document.querySelectorAll('input.ui-input')].find(item=>item.type==='text')?.value})");assert.equal(preserved.email,"factory@example.test");assert.equal(preserved.selected,true);assert.equal(preserved.subject,"Request for estimate · TEST-EST-1");
  await evaluate("(()=>{const button=[...document.querySelectorAll('button')].find(item=>item.textContent.trim()==='Prepare for review');button.click();button.click()})()");await waitFor(()=>evaluate("document.body.innerText.includes('Saving reviewed draft…')"),"Immediate supplier-request progress was not shown",1000);try{await waitFor(()=>evaluate("document.body.innerText.includes('Prepared for review — not sent')&&document.body.innerText.includes('Review the saved Email draft before sending.')"),"Supplier request result and next action missing")}catch(error){const diagnostic=await evaluate("({text:document.body.innerText,html:document.body.innerHTML.slice(0,3000)})");throw new Error(`${error.message}: ${JSON.stringify({postCount,diagnostic,rfqs:(await db.get('SELECT COUNT(*) count FROM supplier_enquiry_drafts')).count,messages:(await db.get('SELECT COUNT(*) count FROM communication_messages')).count})}`)}const result=await evaluate("({text:document.body.innerText,details:document.querySelector('.supplier-rfq__result details')?.textContent})");assert.match(result.text,/Zyle Fenster · request revision 1/);assert.match(result.details,/Approved drawing.pdf/);assert.equal(postCount,2);assert.equal((await db.get("SELECT COUNT(*) count FROM supplier_enquiry_drafts")).count,1);assert.equal((await db.get("SELECT COUNT(*) count FROM communication_messages")).count,1);
  for(const [id,day,body] of [["reply-old","09","Word estimate is being prepared."],["reply-new","10","PDF estimate will follow after review."]]) await repository.save({id,provider:"google_workspace",providerMessageId:id,threadId:"shared-supplier-thread",direction:"inbound",folder:"inbox",status:"received",from:["factory@example.test"],to:["staff@example.test"],subject:`Reply ${day} September`,bodyText:body,sentAt:`2026-09-${day}T10:00:00.000Z`,attachments:[],links:[]});
  await evaluate("[...document.querySelectorAll('button')].find(item=>item.textContent.trim()==='Continue editing')?.click()");
  await evaluate("[...document.querySelectorAll('summary')].find(item=>item.textContent==='Review an incoming supplier reply')?.click()");
  await evaluate("[...document.querySelectorAll('button')].find(item=>item.textContent.includes('Zyle Fenster · Request for estimate'))?.click()");
  await waitFor(()=>evaluate("document.body.innerText.includes('Find and review a supplier reply')"),"Reply selector did not open");
  await evaluate("[...document.querySelectorAll('button')].find(item=>item.textContent==='Find replies')?.click()");
  await waitFor(()=>evaluate("document.body.innerText.includes('Reply 10 September')"),"Newest reply did not render");
  assert.equal(await evaluate("document.body.innerText.includes('Reply 09 September')"),false);
  await evaluate("[...document.querySelectorAll('button')].find(item=>item.textContent.includes('Reply 10 September'))?.click()");
  await waitFor(()=>evaluate("document.body.innerText.includes('PDF estimate will follow after review.')"),"Exact newest body did not open");
  await evaluate("[...document.querySelectorAll('button')].find(item=>item.textContent==='Next page of replies')?.click()");
  await waitFor(()=>evaluate("document.body.innerText.includes('Reply 09 September')"),"Bounded second page did not render");
  await evaluate("[...document.querySelectorAll('button')].find(item=>item.textContent.includes('Reply 09 September'))?.click()");
  await waitFor(()=>evaluate("document.body.innerText.includes('Word estimate is being prepared.')"),"Exact older body did not open");
  await evaluate("[...document.querySelectorAll('button')].find(item=>item.textContent==='Link as acknowledgement')?.click()");
  await waitFor(()=>evaluate("document.body.innerText.includes('Reply linked for staff review')"),"Acknowledgement result missing");
  const responseLink=await db.get("SELECT * FROM manufacturer_response_links WHERE communication_message_id='reply-old'");assert.equal(responseLink.status,"review_required");assert.equal(responseLink.estimate_id,"estimate-1");assert.equal((await db.get("SELECT COUNT(*) count FROM workflow_events WHERE event_name='supplier.quote_returned'")).count,0);
  const old=await repository.findByProviderId("google_workspace","reply-old");await repository.save({...old,attachments:[{id:"old-document",providerAttachmentId:"opaque-word-id",fileName:"Supplier estimate.docx",mediaType:"application/vnd.openxmlformats-officedocument.wordprocessingml.document",sizeBytes:supplierBytes.length,inline:false}]});
  await evaluate("[...document.querySelectorAll('button')].find(item=>item.textContent.includes('Reply 09 September'))?.click()");
  await waitFor(()=>evaluate("document.body.innerText.includes('Supplier estimate.docx')"),"Exact reply document did not render");
  await evaluate("[...document.querySelectorAll('button')].find(item=>item.textContent==='Review and file selected document')?.click()");
  await waitFor(()=>evaluate("[...document.querySelectorAll('button')].some(item=>item.textContent.trim()==='File selected document'&&!item.disabled)"),"Reviewed filing picker was not ready");
  await db.exec("CREATE TRIGGER fail_reply_projection BEFORE UPDATE ON supplier_enquiry_drafts BEGIN SELECT RAISE(ABORT,'disposable linking interruption'); END");
  await evaluate("[...document.querySelectorAll('button')].find(item=>item.textContent.trim()==='File selected document')?.click()");
  await waitFor(()=>evaluate("document.body.innerText.includes('The document was saved, but QuoteSuite could not finish linking it.')"),"Partial saved-file outcome was not visible");
  await db.exec("DROP TRIGGER fail_reply_projection");
  await evaluate("[...document.querySelectorAll('button')].find(item=>item.textContent.trim()==='Retry filing')?.click()");
  await waitFor(()=>evaluate("document.body.innerText.includes('Import Manufacturer Estimate')&&document.body.innerText.includes('Open Files')"),"Saved file actions did not appear");
  assert.equal(storedFiles,1);assert.deepEqual(await readFile(path.join(root,"supplier-estimate.docx")),supplierBytes);
  assert.equal((await db.get("SELECT response_state FROM supplier_enquiry_drafts LIMIT 1")).response_state,"revised_document_received");
  assert.equal((await db.get("SELECT COUNT(*) count FROM workflow_events WHERE event_name='supplier.quote_returned'")).count,1);
  await evaluate("window.addEventListener('quotesuite:import-manufacturer-quote',event=>{window.__reviewDocument=event.detail.canonicalDocumentId},{once:true})");
  await evaluate("[...document.querySelectorAll('button')].find(item=>item.textContent.trim()==='Import Manufacturer Estimate')?.click()");
  await waitFor(()=>evaluate("window.__reviewDocument==='filed-reply'"),"Import handoff lost the saved document identity");
  console.log(JSON.stringify({filing:{providerUploads:storedFiles,partialSuccessVisible:true,retryReusedFile:true,responseState:"revised_document_received",importHandoffDocument:"filed-reply",provider:"disposable adapter"}}));
  await db.run("UPDATE supplier_enquiry_drafts SET revision_request_id='history-request'");
  await evaluate("[...document.querySelectorAll('button')].find(item=>item.textContent==='Open saved review').click()");
  await waitFor(()=>evaluate("[...document.querySelectorAll('label')].some(item=>item.textContent==='After'&&item.querySelector('input')?.value==='Green')"),"Saved review values did not reopen");
  await waitFor(()=>evaluate("[...document.querySelectorAll('legend')].some(item=>item.textContent==='Supplier response reviews')"),"Supplier-specific review controls did not open");
  await evaluate("(()=>{const section=[...document.querySelectorAll('fieldset')].find(item=>item.querySelector('legend')?.textContent==='Supplier response reviews');const select=section.querySelector('select');Object.getOwnPropertyDescriptor(HTMLSelectElement.prototype,'value').set.call(select,select.options[1].value);select.dispatchEvent(new Event('change',{bubbles:true}))})()");
  await waitFor(()=>evaluate("document.body.innerText.includes('Save supplier review')"),"Supplier field review did not render");
  await evaluate("(()=>{const section=[...document.querySelectorAll('fieldset')].find(item=>item.querySelector('legend')?.textContent==='Supplier response reviews');for(const [label,value] of [['Field','finish'],['Before','White'],['Requested','Green'],['Returned','Green'],['Before source / page','Issued page 1'],['Returned source / page','Supplier page 2']]){const input=[...section.querySelectorAll('label')].find(item=>item.textContent===label).querySelector('input');Object.getOwnPropertyDescriptor(HTMLInputElement.prototype,'value').set.call(input,value);input.dispatchEvent(new Event('input',{bubbles:true}))}})()");
  await evaluate("(()=>{const button=[...document.querySelectorAll('button')].find(item=>item.textContent==='Save supplier review');button.click();button.click()})()");
  await waitFor(()=>evaluate("document.body.innerText.includes('Supplier review saved.')"),"Supplier review result was not visible");
  assert.equal((await db.get('SELECT COUNT(*) count FROM supplier_response_reviews')).count,1);
  const savedSupplier=await db.get('SELECT checks_json FROM supplier_response_reviews');assert.equal(JSON.parse(savedSupplier.checks_json)[0].afterValue,'Green');
  await evaluate("[...document.querySelectorAll('summary')].find(item=>item.textContent.includes('supplier review versions')).click()");
  await waitFor(()=>evaluate("document.body.innerText.includes('Showing 1–1 of 1 saved review snapshots')"),"Supplier-specific review history did not open");
  await evaluate("[...document.querySelectorAll('summary')].find(item=>item.textContent.startsWith('Supplier review saved on')).click()");
  await waitFor(()=>evaluate("document.body.innerText.includes('Before source: Issued page 1 · Returned source: Supplier page 2')"),"Supplier history lost retained source references");
  await evaluate("[...document.querySelectorAll('summary')].find(item=>item.textContent.includes('earlier review history')).click()");
  await waitFor(()=>evaluate("document.body.innerText.includes('of 12 saved review snapshots')"),"Persisted review history did not load");
  assert.equal(await evaluate("document.body.innerText.includes('Showing 1–10 of 12')"),true);
  await evaluate("[...document.querySelectorAll('button')].find(item=>item.textContent==='Older reviews').click()");
  await waitFor(()=>evaluate("document.body.innerText.includes('Showing 11–12 of 12')"),"Bounded history pagination failed");
  await evaluate("[...document.querySelectorAll('button')].find(item=>item.textContent==='Verify changes').click()");
  await waitFor(()=>evaluate("document.body.innerText.includes('Temporary review interruption')"),"Review failure was not visible");
  assert.equal(await evaluate("[...document.querySelectorAll('label')].find(item=>item.textContent==='After').querySelector('input').value"),'Green');
  await evaluate("[...document.querySelectorAll('button')].find(item=>item.textContent==='Open other review').click()");
  await waitFor(()=>evaluate("[...document.querySelectorAll('label')].find(item=>item.textContent==='After')?.querySelector('input').value==='Blue'"),"Switching request retained another request's form");
  console.log(JSON.stringify({reviewForm:{savedChecksReopened:true,failurePreservedValues:true,requestSwitchIsolated:true,history:{persisted:true,pageSize:10,total:12}},scope:'Production review component and history HTTP route with disposable fixture inputs; not the complete application reissue journey'}));
  const userRuntimeAfter=await fetch("http://127.0.0.1:3001/api/health").then(response=>response.ok?response.json():null).catch(()=>null);if(userRuntimeBefore)assert.equal(userRuntimeAfter?.instanceId,userRuntimeBefore.instanceId);else assert.equal(userRuntimeAfter,null);console.log(JSON.stringify({previewOnly:true,recoverableFailure:true,preserved,immediateProgress:true,duplicateProtection:{postCount,records:1,messages:1},resultNextAction:true,replySelection:{sameThreadIndependent:true,boundedPages:true,exactBody:true,acknowledgementPersisted:true},apiBaseline:userRuntimeBefore?"preserved":"not listening and unchanged"},null,2));
}

run().catch(error=>{console.error(error);process.exitCode=1}).finally(async()=>{try{socket?.close()}catch{}try{const cleanup=await controller.stop("final");console.log(`Supplier RFQ browser cleanup: ${JSON.stringify(cleanup)}`)}catch(error){console.error(error);process.exitCode=1}if(http)await new Promise(resolve=>http.close(resolve));if(db)await db.close();if(root)await rm(root,{recursive:true,force:true})});
