# In-progress Roadmap completion programme

## 13 September — travel evidence audit and incomplete-route warning

The saved programme currently substitutes zero for absent route values and uses outward distance twice for return mileage. Changing that arithmetic directly could reprice existing calculations. The bounded correction adds an explicit Travel time not confirmed / Travel costing is incomplete review warning unless outward time/distance have a retained route identity or explained manual override. Additive travelEvidence uses nullable values and explicitly leaves return confirmation unresolved. Existing cost arithmetic and historical snapshots remain unchanged; this is not completion of reviewed return-route costing.

Seven focused route/projection tests, twenty-six existing programme tests and full typecheck pass. Missing/invalid values, unexplained overrides and legitimate retained zero-distance routes are distinguished. Three old workforce-only fixtures failed on a missing canonical_documents table; they now include the empty id/web_view_link lookup needed by current workforce listing. This is fixture compatibility, not provider-document verification. No new browser acceptance is claimed. No browser/API infrastructure was started or live records changed; the existing user watcher remains owner of the API (observed PID 39260, parent 29604, runtime instance 82321a51-6239-44bd-a3a7-c0489c11983e after source reload).

Existing survey-delivery-installation stays partial/unaccepted with unchanged counts and original-73 mapping. Exact next dependency: reviewed directional route snapshots tied to the selected installer/team base and site, explicit return leg/manual basis, and a snapshotted installer travel policy that chooses daily return versus one outward/return around overnight stays without silently repricing older scenarios. The unrelated ConfigureInstallation.tsx worktree edit is preserved. Drawing/support, survey approval and reviewed delivery also remain open. Existing AGENTS travel/immutable rules apply. Local checkpoint only; push remains permission-blocked.

## 13 September — saved membrane width and area allowance

ME508 and ME501 calculations now retain explicit rollWidthMm from their selected catalogue snapshot. Installer material projection uses required linear metres × width once, with the linear calculation's existing contingency. The PDF calls this Membrane area allowance and explains that it is not net installed coverage or whole-roll purchased area. Missing/invalid width or length stays unknown; product labels are not parsed for geometry. Required roll quantities, purchase costs and pricing remain unchanged. Later Administration changes do not rewrite the retained calculation or previously prepared documents.

Seven focused projection/PDF checks, forty-two existing installation-material rule/snapshot/API regressions and full typecheck pass. Normal WinPro browser session 78286 exits 0 through twelve-position genuine-source import/repeat/reload, ordinary ME508 250 mm selection, document preparation failure/retry, client separation, history paging/retry and smaller-screen actions. It verifies saved projection width 250 mm, area equal to saved linear requirement × 0.25, and the exact square-metre value and basis in independently read PDF. Installer PDF SHA256 3df142e0167ff9def1b20949ab7512c7a431ed0956a1c10614294297cf3e46b6 has six pages. All twelve manufacturer weights/openings remain passing. Shared finally cleanup performs independent post-cleanup process/profile counts: both zero; user API 87691867-55b0-49b6-8af6-c5411032cfe3 remains unchanged during acceptance under its existing watcher.

Existing survey-delivery-installation stays partial/unaccepted with unchanged original-73 mapping and counts. Next: support/material details, drawing selection and installer travel/crew workflow, while genuine missing-weight normal-route coverage remains open. Survey approval and reviewed delivery remain incomplete. No live data/provider/email changes. Existing AGENTS material-snapshot and unknown-evidence rules apply without duplication. Local checkpoint only; push remains permission-blocked.

## 13 September — positive WinPro normal-route installation evidence

The shared owned browser journey now supports a separate immutable WinPro source profile using --stop-after-winpro-installation-documents; the existing WEB source mode remains available. Both profiles retain explicit reviewed source hashes, Position counts and commercial totals rather than deriving test expectations from the output being tested.

Run aa4bcf7a-e500-4855-bb90-7105cde5517e exits 0 on byte-identical source be0d783701c6286b639dfd1da7cb55228fd591fbd5334d36751ce8022720d5e6. Normal upload → identity/extraction review → final import → repeat import → reload retains twelve canonical Positions, source bytes/visual ownership and GBP 5989.85 without duplicate source/Position/costing identities. Original issued evidence remains Issued. Normal ME508 selection and installer document preparation produce twelve EKO-OKNA identities, twelve unit weights, twelve Windows and exact 001 opening text 1.01: Side Hung - Turn; 2.01: Fix in frame. Independently inspected installer PDF hash 15621798182dabec6fa8db0374dc30d9e05f5e48caf8c679d7fd730459f93813 has six pages; client price-free output has three. Preparation failure/retry, eleven-document history pagination failure/retry and 960x600 action access pass.

Exact owned root 41604 and profile run-3oyzVh are absent after cleanup, independently checked; temporary database is absent. Existing user API 5f626e90-8964-40ef-bc52-58403854b9cc remains unchanged during the run. No live delivery, provider or business-record mutations occurred. Existing survey-delivery-installation technical evidence advances without status/count or user-acceptance promotion. Remaining continuation: missing-weight normal-route acceptance and mixed-field review, material area/support, drawing selection, installer travel/crew, survey approval and reviewed delivery. This is not full installer-pack acceptance. Existing AGENTS governance needs no duplicate rule; checkpoint remains local while push permission is blocked.

## 13 September — source-field installer opening projection

Read-only WEB source inspection confirms no explicit opening field in its retained text; hardware-angle notes and drawings cannot substitute. Existing WinPro source evidence does contain sash-specific Fitting and explicit Fix in frame Profile fields. Installer projection now uses those exact source IDs, matching raw values, sash sections, source pages and field references through the shared Position/quantity/dimension guard. Missing parts remain Not confirmed; duplicate references or stale dimensions reject the fallback. Explicit revision opening values retain precedence, client output is unchanged, and no issued document is rewritten.

Seven focused tests and full typecheck pass. Genuine WinPro PDF extraction through the installer renderer independently verifies 1.01: Side Hung - Turn; 2.01: Fix in frame. Tests cover partial missing fields, duplicate references, stale dimensions, source preservation and explicit reviewed opening precedence. This is genuine extraction/service/generated-PDF evidence, not positive WinPro final-import/normal-browser acceptance. No browser/API infrastructure was started for this turn and no new browser pass is claimed.

Existing survey-delivery-installation remains partial/unaccepted. Counts and original-73 mapping are unchanged. Next: positive multi-source normal-route acceptance for retained openings and missing-weight fallback, then material area/support, drawing selection, installer travel/crew, survey approval and reviewed delivery. The WEB source's absent opening description remains a source limitation requiring review, not a reason to fabricate it. Live business/provider/email data remains untouched. Existing AGENTS evidence/immutable/guided-UX rules apply; local checkpoint only while push permission remains blocked.

## 13 September — exact-source installer manufacturer/system projection

Installer manufacturer details were blank because the basic canonical schedule does not retain manufacturerName, while the saved supplier row does. Manufacturer/system fallback now uses the same shared exact Position ID, quantity and dimension guard as weight, and requires a retained source-row identity. Only the explicit manufacturer role and supplied product system are projected; dealer/issuer names and arbitrary source objects are excluded. Explicit revision values retain precedence. Client price-free output and existing saved documents/Order plans are unchanged.

Eleven focused manufacturer/weight/fallback tests and full typecheck pass. Normal-browser run 8b3a5a79-d604-4276-b246-a9493402b288 exits 0 through genuine PDF upload/review/import/repeat/reload, ME508 selection, document failure/retry, separate audiences, history and 960x600 access. All five imported Positions retain EKO-OKNA, their source weights and category totals. PDF visual review confirms EKO-OKNA / Reynaers MASTER LINE 8. Installer PDF SHA256 827c5f89bfe71a2c337b1791aa07130e0bf288ecbedadf98518c902fc5421c24. Browser/profile cleanup is zero and independently rechecked against exact root 40728 and profile run-rUgBQI; temporary database is absent. User API c599c41c-3934-4ceb-b43b-918fc5024b47 remains unchanged during acceptance under its existing watcher.

Read-only examination also confirms the existing positioned extractor returns no structured sash/opening fields for the WEB layout. Those openings remain Not confirmed, not inferred from neighbouring text or drawings. Existing survey-delivery-installation remains partial/unaccepted with unchanged counts and original-73 mapping. Next: retain deterministic opening evidence where available, complete missing-weight normal-route/field review, then material area/support, drawings, installer travel/crew, survey approval and reviewed delivery. No live records, provider files or emails changed. Existing permanent source/role/immutable/UX rules apply without duplication. Local checkpoint only; push remains permission-blocked.

## 13 September — actual retained glazing-field contract correction

Read-only extraction of the genuine EKO WinPro source showed canonical glazing points to Glazing required in the Glazing section, not the Glazing label used by the initial fallback fixture. The installation fallback now accepts that exact source field contract while preserving field ID, raw value, source page and source-row checks. A similarly labelled message is rejected. Manufacturer weight still takes precedence; no source field, issued snapshot or live import is modified.

Twelve focused weight/fallback tests and full application typecheck pass. Actual PDF extraction proves the first Position has two uniform glazing units, 1227 × 1265 mm envelope and three 4 mm panes: the fallback helper produces 46.56465 kg glass only once, while the normal projection retains the stated 68 kg unit weight. The initial assertion contained a hand-calculation typo, corrected without changing calculator arithmetic. A controlled missing-weight copy exercises fallback selection and wrong-section rejection; this is explicitly service fault coverage, not a byte-identical missing-weight import/browser pass. No browser/API infrastructure was started this turn and no new normal-route acceptance is claimed.

Existing survey-delivery-installation remains partial/unaccepted with unchanged original-73 mapping and totals. Next: source-backed fallback normal-route acceptance where genuine missing-weight evidence exists, then mixed-field/laminate review, remaining manufacturer/opening projections, material area/support, drawings, travel/crew, survey approval and reviewed delivery. Existing AGENTS rules already cover this source-preserving correction. Local checkpoint only; the prior push permission restriction remains unresolved.

## 13 September — retained supplier categories in installation schedules

Follow-on inspection found the document projection reads productClass/productType, while imported canonical Positions retain the explicit source product marker in product. A bounded fallback now accepts recognised explicit supplier product labels from source-owned Positions, with existing explicit categories taking precedence. It never classifies from prose/system names or the historical positionType default, which the importer sets to Window even for doors. Missing category evidence remains visible instead of becoming an invented Window. No source schedule, Order or issued PDF is rewritten.

Twelve focused projection/source/weight checks pass, including five distinct categories, imported Door despite default Window, unknown source descriptions, source preservation and accepted Order ownership. Full typecheck passes. Normal-browser run 1d143653-0eaf-43ce-986d-0dd03e36af37 exits 0 through genuine PDF upload/analyse/review/import/repeat/reload and installer document preparation, failure/retry, ME508, separate client PDF, history and small viewport. Five explicit supplier Windows now appear in saved totals; the three unrelated fixture Positions lack category evidence and remain unconfirmed. Visual PDF review confirms Windows 5 / Category not confirmed 3. All five manufacturer unit weights remain source-page verified. Installer PDF SHA256 6de5e9705407a1cb8c237544ff1bd9b38769685ba9e3122266f97738b4e9aaf7. Independent post-cleanup inspection confirms root PID 33660 absent, exact run-2VQQDR profile absent with zero matching processes, and temporary database absent. User API a3df8f6f-70b7-4d08-8e98-3bb0be032481 remains unchanged during acceptance, PID 13996 under the existing user watcher 29604; no manual listener replacement occurred.

Existing survey-delivery-installation remains partial/unaccepted; counts and original-73 mapping are unchanged. Exact continuation: complete source-backed glazing/field fallback acceptance and missing manufacturer/opening/category projections where explicit evidence exists, then material area/support, drawings, installer travel/crew policy, survey approval and reviewed delivery. Do not use legacy default positionType as source truth or reinterpret issued snapshots. No live business/provider/email changes. AGENTS already governs source preservation, unknowns and audience separation. Local checkpoints only; the prior push restriction remains unresolved.

## 13 September — genuine-source manufacturer weight through installer PDF

The first genuine-source weight browser check failed after otherwise successful import/document preparation: the EKO WEB itemised adapter retained only Colour specification evidence, although the original PDF explicitly contains five Unit weight rows. This was not a missing PDF value or a reason to bypass Position ownership. Fresh extraction now reuses the existing positioned-table interpreter on the uniquely owned Position page, promoting only a single explicit positive kilogram Unit weight field. Missing headings, conflicting fields, non-kg units and multi-Position pages remain unconfirmed; continuation-page totals cannot substitute. Interpretation is versioned eko_web_itemised_colour_weight_v2. No live import, issued evidence or existing saved document is rewritten.

Ten focused source/weight/PDF tests pass, five genuine-source/API tests pass including final import/reload and injected commercial rollback, six glass/pagination regressions pass, and full application typecheck passes. Normal-browser run 53ae2282-7175-44c0-826a-98a617b52f57 exits 0 using a byte-identical disposable source (SHA256 d1f34d3fd36ef40e4fb1b3ccbddc96b96837fdfd86f598af9c2b189f674f1899). Upload → analysis/review → final import → repeat import → reload → normal ME508 selection → installer preparation retains all five canonical Position/source-page weights: 96.8, 182.1, 182.1, 74.9 and 50.9 kg per unit. Independently read PDF SHA256 0d786d2e184ae6e830ef62a5c4c2848b926983cbc14759244f7eb458d511b0ee contains each value. Failure/retry, separate client PDF, eleven-document history and 960x600 actions also pass. Exact browser/profile remnants are zero; the existing user API 99d633d8-1fcd-4c3e-b719-41be48bf2c8e is unchanged during the run. Its existing native watcher restarted after the source edit, not through manual API replacement.

Existing survey-delivery-installation evidence is updated without status, original-73 mapping or user-acceptance promotion. PDF visual review also exposes unconfirmed schedule categories in this source journey; inspect exact source/category mapping next, without guessing from product descriptions. Complete glazing/field fallback, material area/support, drawings, installer travel/crew, survey approval and reviewed delivery remain open. No live records/provider files or communications changed. Existing AGENTS evidence/unknown/immutable rules apply, so no duplicate permanent rule is needed. Push remains permission-blocked; local checkpoint only.

## 13 September — source-backed uniform glazing fallback connected

When no manufacturer weight is supplied, installer Position projection now maps an exact retained Glazing field/page/source row into the shared strict fallback. Supported explicit pane/spacer notation is bounded to the existing source form (for example 4th/14Ar/4/14Ar/4th plus recognised thermal/acoustic/thickness metadata); only glass panes contribute weight, never spacer widths or whole-IGU thickness. Source text must match the canonical field exactly, and all retained glazing units must have the same pane thicknesses. The outer scheduled envelope is counted once, irrespective of split count or grouped Position quantity. Non-rectangular geometry, mixed fields, laminate shorthand, unsupported/alternative notation and missing provenance remain unresolved. Ambiguous manufacturer weight is not silently replaced by a fallback. Mixed fields still need explicitly mapped geometry before their supported arithmetic can be used.

Eleven focused tests and full application typecheck pass. Tests use the existing source specification shape and explicit build-up text, preserve inputs, exclude spacer/quantity duplication, reject conflicting and incomplete evidence, and independently read the PDF for 60 kg glass only, approximate dimensions, source page and missing-component warning—not 60 kg complete-unit weight or a lifting approval. This is source-shape/service/PDF evidence, not an exact supplier-file save/import/reload browser pass. No new browser run is claimed for this mapping. Previously prepared PDFs and immutable Order plans remain unchanged; new documents retain the selected projection through the existing store.

Next: genuine source-backed disposable manufacturer weight/fallback save → prepare → reload acceptance, then explicit mixed-field geometry/laminate thickness review where needed. Remaining material area/support, drawings, travel, survey approval and delivery remain open. Existing survey-delivery-installation stays partial and unaccepted. Push remains permission-blocked; local checkpoint only. No live data/provider/email mutation occurred. Existing evidence, missing-input and lifting-warning rules apply without a new AGENTS duplicate.

## 13 September — source-bound manufacturer unit weight in installer output

Installer Position output now resolves weight only from one exact saved calculation row with matching canonical Position ID, dimensions and quantity. The existing manufacturer canonical weight must resolve to one retained Unit weight field with agreeing numeric evidence, a source page and source-row identity before it is presented as kg per unit. Ambiguous totals/bases remain manufacturer-stated weight with per-unit basis unconfirmed, never divided or multiplied into an assumed unit weight. Missing/mismatched evidence remains Not confirmed. Source page and a handling warning are shown in the PDF; internal source identities remain in the retained projection/binding, not printed. Client schedules omit this installation-only evidence. Existing saved documents and plans are not rewritten.

Eleven focused projection/PDF/pagination checks and full typecheck pass. The source fixture follows the actual existing canonicalSpecification/sourceSpecification Unit weight shape; it is not an exact supplier-document import test. It covers quantity three without multiplying 84.5 kg, source mismatch/absence/ambiguous label, client exclusion and independently extracted PDF source/warning text. Normal browser run e505463f-532a-4a53-bbae-db9ddcdf09a0 exits 0 through material selection, document preparation/retry, PDF audiences, history and smaller-screen access after the renderer change; exact browser/profile remnants are zero and user API d9792887-6a3a-453e-9736-06808feec74f is unchanged. That browser fixture lacks positive manufacturer weight, so it is regression/unknown-state integration evidence, not a source-positive weight acceptance claim.

Next: map retained complete glazing/field evidence to the strict glass-only fallback, preserving manufacturer evidence precedence and explicit uncertain bases; verify a genuine source-backed disposable Position through save/prepare/reload, including conflicts. Remaining membrane area/support, drawings, installer route, survey approval and delivery also stay open. Existing survey-delivery-installation remains partial/unaccepted. Push remains permission-blocked; local checkpoint only. No live business records, issued evidence, provider files or emails were changed. Existing evidence/unknown/handling rules cover this work without another AGENTS addition.

## 13 September — strict glass-weight fallback foundation

The existing glass tool's pane arithmetic now lives in one shared module, with the tool retaining its existing shape, pane and missing-input behaviour. A separate installation-only boundary reuses that arithmetic but rejects absent/zero/nonfinite pane thickness, unknown laminate thickness, missing dimensions, unsupported non-rectangular geometry, empty fields, overlaps and fields outside the unit envelope. Explicit non-overlapping fields are summed once; the outer envelope is not added again. Output is always labelled Estimated glass weight using approximate dimensions and explicitly excludes a verified complete-unit/lifting load. Inputs are not mutated and no pane thickness is guessed from a laminate code or glazing label.

Three focused tests and full application typecheck pass. After a process-spawn restriction, Node's in-process test mode passed without a new server or external access. Floating-point arithmetic remains unchanged; the laminate assertion uses a numerical tolerance rather than changing the calculator to satisfy an exact decimal comparison. This is a shared arithmetic and strict service foundation, not source mapping or installer-document acceptance. No API/UI document path calls the new fallback yet, and no new normal-browser weight claim is made.

Next: bind weight evidence to exact saved Positions/revisions, prefer source-referenced manufacturer unit weight only where its per-unit basis is explicit, retain ambiguous manufacturer weight as review-required, and map complete known glazing/field evidence into the strict fallback. Then project the evidenced/estimated/unknown distinctions into the installer PDF and verify saved/reloaded normal routes. Do not infer per-unit weight from a quotation total or use current mutable evidence to replace a released snapshot. Existing survey-delivery-installation stays partial and unaccepted. Existing AGENTS source/unknown/lifting and industry-module rules apply; no duplicate permanent requirement is added. No live business data, issued records, provider files or communications changed. Push remains permission-blocked; local checkpoints continue.

## 13 September — material PDF pagination repair

Material heading, specification, quantity, basis and review now form one table group. Compact groups stay on one page; long or multiline retained evidence can flow with a repeated material heading rather than being clipped or discarded. No source text or prior generated PDF is rewritten. A small follow-on wording correction removes internal fixing keys from printed headings, retaining familiar material names and genuine product codes.

Ten focused generated-PDF/material tests pass, including eighteen groups across multiple pages, long source evidence retained through its final review, repeated heading context and no internal substrateFixings heading. Full application typecheck passes. Normal browser run df6f1785-1845-4e7f-817a-e2f6779ef772 exits 0 through ME508 selection, snapshot equality, preparation failure/retry, separate PDF audiences, history and smaller-screen action access. User API instance 16e4aeb7-2d36-453a-8684-2dbf993852a8 stays unchanged; exact owned browser/profile remnants are zero. Visual inspection of its final-page raster confirms the formerly isolated Review row now remains with its material heading and specification. The internal-key wording correction after this run has generated-PDF test evidence, not a second browser run.

Existing survey-delivery-installation stays partial and unaccepted. The print-grouping defect is technically corrected; next are remaining membrane area/support details, Position weight evidence/fallback, drawings, installer travel, survey approval and reviewed delivery, plus remaining stale-source recovery coverage. Push remains permission-blocked as recorded above; retain local checkpoints without retrying external egress. No live business, provider or email writes occurred. Existing print/guided-UX and evidence rules apply; no duplicate AGENTS requirement was added.

## 13 September — normal-route material selection-to-document verification

Checkpoint/push restriction: the session permission reviewer rejected the combined git checkpoint/push operation because it could not verify external source/roadmap egress to the remote. A local-only checkpoint was permitted. The read-only remote check shows origin https://github.com/ecofenster/QuoteSync.git, on the existing backup/post-phase-9-working-2026-08-04 branch. Do not retry or route around the denied push without the required destination approval; continue independent work and retain local checkpoints. Last confirmed pushed checkpoint before this restriction is 503bc2f.

The shared Estimate document acceptance journey now opens the normal Project Costing Installation Materials section, requires ME508 and selects the current 250 mm variant through its ordinary controls before opening Files / Documents. Run 0a13eecb-9f69-4015-aa4e-1a6a274f576c passes selected variant/basis PDF checks, preparation failure/retry, both audiences, eleven-document history and 960×600 action reachability. A strengthened run first exposed an incorrect test query against the scenarios table; options are stored in project_calculator_lab_options. No application schema or data guard was changed to accommodate that mistake.

Corrected strengthened run 4a965ad1-6b53-495d-999c-28728646ed52 exits 0 and proves exact equality between the UI-saved ME508 material snapshot and retained installer projection for provision quantity and required linear metres. The downloaded installer PDF includes ME508, 250 mm and the 15% linear basis; every retained client projection omits the installer section, and client PDF checks exclude operational allowances. User API instance 8f9eb069-72e5-41c4-b0a2-fb81df82eb77 remains unchanged during this final run. Exact owned browser/profile remnants are zero. This closes the selected-ME508 normal-route verification gap, not all material variants, full installer packs or user acceptance. Earlier interrupted/runtime-failed runs are not promoted.

Visual inspection of the generated final-page raster found readable text but an isolated material Review row continuing onto a new page without its material heading. Record a bounded print-grouping correction before ordinary-user pack acceptance; do not describe the PDF layout as fully accepted. Next: keep small material specification/quantity/review groups together, then remaining area/support/weights, drawings, travel, survey and delivery. Existing survey-delivery-installation remains partial and unaccepted. No new AGENTS rule is needed. Test changes and read-only persisted checks used only fresh disposable records; no live email, provider write or business-data mutation occurred.

## 13 September — material specification and quantity basis

The material calculator now returns the exact selected bracket/frame-screw/substrate-fixing identity and label alongside its existing quantity evidence. This is additive metadata, not a new selection or pricing algorithm. Installer projection exposes the retained selected specification, never the catalogue cost; absent historical labels remain Not confirmed. PDF rows explain the recorded quantity basis: saved perimeter plus linear contingency and rounding for linear materials, retained FM330 depth/width/yield/box assumptions without a second contingency, fixed one-per-order tools, and per-Position fixing quantities with whole-pack rounding. Missing fixing rules still produce unknown quantities even when a product is selected.

Ten focused document/material/Order tests, all 42 existing material-rule tests, and full application typecheck pass. Tests include explicit selected-versus-other bracket identity, unknown historical specification, actual FM330 box/can quantities and distinct basis, commercial exclusion, accepted-scope recalculation and unchanged source evidence. The wider suite covers saved-cost snapshots, explicit current-catalogue adoption, rollback and supplier-installation substitution. No new normal-browser material-selection acceptance is claimed; the earlier watched-runtime gate remains open for that work. Existing user supervisor PID 29604 remains in place; no user API was deliberately replaced or stopped, and no live business/provider/email mutation occurred.

Existing survey-delivery-installation remains partial/unaccepted. Continue with normal-route material selection-to-PDF, membrane area/support specification, position weights, drawings, travel and survey/delivery. Old issued PDFs and saved Order plans are not rewritten. No new AGENTS rule is needed: saved catalogue authority, exact evidence and commercial exclusion already govern this implementation.

## 13 September — installer material quantity projection

Installer drafts now include an allowlisted operational materials section from the saved calculation: required sealing/tool items retain their provision unit, roll/can quantities and linear requirement; fixing and packer quantities retain explicit unresolved states. No purchase prices, unit costs, selling values or margins are copied. Client price-free schedules omit this section. Reviewed Order proposals recalculate materials for only the exact accepted operational scope using saved rules/catalogue/options; the sold scenario is unchanged. Previously retained plan/document projections are not backfilled or rewritten.

Nine focused checks and full application typecheck pass. Tests call the actual material calculator, verify ME508 linear metres/roll provision and an order-fixed AA270 quantity, independently read both generated PDF audiences for quantities and commercial exclusion, preserve unknown geometry, and prove an accepted alternative's material scope excludes unrelated sold Positions. The initial fixture lacked canonical frame-perimeter evidence and correctly returned unknown; the fixture was corrected to supply it, without adding an invented production fallback. Square-metre quantities are not available from this source projection and remain unconfirmed; do not derive them from an unreviewed label. Complete fixing variant detail, membrane area basis, weights and support/Purenit remain incomplete.

Normal-browser run b40e187f-e4df-4d5d-a47e-1739899f6621 completed Order review/save/history/reload/PDF steps and exact browser/profile cleanup (zero remnants), but its final gate failed because the pre-existing native watcher changed the user API instance from 7c0a7c7c-9e4a-4869-93df-c25180c5a1b8 to a4e4b82e-2a92-4249-a142-7653b47e4920. This is not a full browser acceptance pass. That fixture does not assert a selected ME508 workflow; material quantities have service/PDF evidence only. No repeated blind rerun, live email, provider upload or live business mutation occurred. Existing survey-delivery-installation stays partial and unaccepted. Next: normal-route selected material evidence and full gate on a stable watcher, plus saved-material specification/basis, weights, drawings, travel, survey approval and delivery. Existing AGENTS material/evidence rules apply; no duplicate permanent rule is required.

## 13 September — bounded reviewed Order-plan history

The existing installation document panel now offers View all reviewed Order plans, with ten-version pages, an accurate Showing count and Older/Newer controls. History is scoped to the exact Client/Estimate/Order; the route checks canonical Order ownership before querying. It returns summary metadata only, not internal proposal or costing JSON. A failed page load retains the prior page and selection for retry. Selecting an older plan adds that exact identity to the bounded twenty-choice selector; context refresh resolves it explicitly and rejects unavailable or unrelated plans instead of selecting a replacement. Immutable history and source evidence are unchanged.

Full application typecheck and the extended disposable plan-store test pass. The latter verifies 22 versions, deterministic descending pages, old-plan identity, scope isolation, invalid offsets and no private proposal projection, alongside existing idempotency, source-change rejection and rollback checks. Normal browser run a6cf70a5-924f-4426-bb38-056635710702 exits 0 through disposable customer acceptance/Order/PDF, reviewed plan save, 22 versions created through the real guarded API, full reload, three history pages, forced paging failure/retry, selection of plan 1 outside the recent twenty, retained details and installer PDF. Existing read-failure/retry and sold-costing preservation checks also pass. Exact owned browser/profile remnants are zero; user API instance e9e6d5b8-3367-4240-b5a4-b2b3bb590a96 remains unchanged during the run. No live records, email or provider storage writes were used.

Existing survey-delivery-installation remains partial and not user-accepted. Next: stale-source browser recovery, then complete installer pack operational content, drawings, survey approval and reviewed delivery. Ordinary-user review: Orders → accepted Order → installation documents → View all reviewed Order plans → Older plans → Use the desired plan → View saved plan details → Prepare draft PDF. No new permanent rule is needed; bounded loading, guided feedback and exact immutable revision rules already cover this work.

## 13 September — saved Order-plan review and full-reload gate

The existing Order document panel now exposes View saved plan details for the exact selected immutable plan. It reads the existing Client/Estimate/Order-scoped endpoint and shows the retained review reason/date, accepted revision, site, team, crew/days, Position scope and outstanding information. Details load only on disclosure; failed reads preserve the selection and offer Retry, and stale asynchronous responses are ignored. No new record, source refresh, cost mutation or delivery occurs. The display explicitly distinguishes saved review from current site confirmation and directs the user to draft PDF review.

Full application typecheck passes. Initial browser run bdad978a-5279-4559-ab9c-0b4a43e7cfe4 stopped because the action helper did not handle native summary controls; the test was corrected, not the working disclosure UI. Final normal-route run 17f40743-2a75-4def-b7ef-f4d1d87a8a1a exits 0: customer acceptance, exact Order entry, client PDF, Order plan preview, forced save failure/retry, one immutable saved plan, installer PDF, unchanged sold scenario, full application reload, selected saved plan, forced details-read failure/retry, retained reason and every accepted Position, and another PDF without reselecting the old scenario. Exact owned browser/profile remnants are zero. The user API instance 358d62ce-2954-4320-8fd4-eaed22262de4 is unchanged during this final run. This closes the previously outstanding full-reload runtime gate for this journey; it does not prove adoption of the new watcher, which remains pending in the existing session.

Existing survey-delivery-installation remains partial and not user-accepted. Next bounded tasks: older-plan discovery with bounded loading and stale-source browser coverage, then complete operational pack materials/weights/drawings/travel, survey approval and reviewed delivery. No new permanent rule is needed; the existing guided UX, exact revision and immutable evidence rules apply. Ordinary-user acceptance: open a disposable accepted Order → installation documents → select its reviewed plan → View saved plan details → review the retained scope and warnings → Prepare draft PDF → open and check it. No live email, provider upload or business-data write occurred.

## 13 September — watcher recovery verification

Checkpoint e1431d3 is pushed. The focused runtime suite now has six passing tests, including a real owned child recovering after imported dependency deletion/restoration and a syntax error/correction. Each replacement follows the preceding child's exit; repeated stop calls leave no child alive, and later file edits do not restart a stopped watcher. All files and processes are disposable and cleaned in finally. This adds technical recovery evidence to platform-web, not user acceptance or adoption in the existing native-watch session. The Order full-reload runtime gate remains open. No new permanent rule is required.

Read-only runtime comparison found the existing native-watch supervisor still at PID 29604, but its API instance changed from f80cdee1-4a5e-4ec3-bcdf-4ad46114583c to 358d62ce-2954-4320-8fd4-eaed22262de4 during this work. Codex did not stop or replace it. Therefore this session does not claim unchanged user API instance evidence; the disposable watcher tests pass independently, while rollout remains outstanding.

## 13 September — content-aware development watcher implemented

The platform-web prerequisite now replaces native --watch for future combined-supervisor starts with a content-aware owned watcher. It tracks actual require/import dependency reports from the installed Node runtime, watches their directories for signals and compares settled SHA-256 content before restart. An early test exposed transient truncation during an identical rewrite; comparison now happens after debounce rather than treating intermediate bytes as restart evidence. Restarts serialize termination of the exact owned child before replacement. API/runtime ownership and occupied-port refusal remain unchanged.

Five focused checks pass: metadata touch and identical rewrite do not restart; a real imported dependency edit restarts once; deletion/restoration are detected; read failure preserves previous evidence; the actual combined supervisor restarts a temporary-port API and restores its empty baseline, and refuses an existing listener. Tests use disposable files/processes, not the user API. The installed Node WATCH_REPORT_DEPENDENCIES report contract is exercised by the real-child test, not assumed from a mock. AGENTS now records content-aware watch semantics as a permanent runtime rule.

The existing user supervisor PID 29604 still runs native --watch. No current user process was replaced, and the Order full-reload unchanged-runtime gate remains open until the corrected watcher is active and that journey is rerun. New npm run dev:quotesuite starts use the new watcher. Do not bypass occupied-port refusal or silently kill the existing supervisor to apply it. Next: controlled ownership-preserving adoption of the corrected watcher, then repeat the Order reload gate; independent installation document/UX work can continue meanwhile. Existing platform-web status remains in progress with technical verification separate from rollout/user acceptance.

## 13 September — observed cause of acceptance watch restarts

Read-only investigation now narrows the restart mechanism. Fresh isolated database initialisation alone produced no observed repository change events and did not restart the API. During the observed full Order-plan run `72ef80ff-e4a3-434f-bc07-096d1fb3f771`, Windows emitted change events at 04:14:37 UTC for installationQualificationEvidenceService.js and workforceDocumentMigration.js, while their modification times remained 02:12:59 and 02:11:39 UTC. Both current file hashes exactly match HEAD (`f5055071e7db279e4535f284640e606382a220e2` and `58d9400f9181043ba781f274357c8e30583e8248`). The installed Node internal/watch_mode/files_watcher source confirms that imported-file events trigger restart without content comparison. This supports content-preserving file notifications as the trigger; the OS/tool responsible for the notification is not proven. Do not claim a content edit or blame database writes.

The functional Order review/save/PDF/reload journey again passed, with zero exact owned browser/profile remnants, but the unchanged-user-API gate correctly failed. The user watch supervisor remains PID 29604; Codex did not replace or stop it. The checked-in observer only reads events/metadata and optionally creates one fresh disposable database; it does not launch browsers or control APIs. Use --observe alongside the existing shared browser runner when further observation is required.

Next bounded prerequisite under platform-web: make development watching content-aware so unchanged-file notifications cannot restart the API, while real imported server/shared changes still restart once and owned-process cleanup remains intact. Test that watcher on disposable processes before changing the normal supervisor start path. Applying a changed watcher to the current user-owned session needs a controlled, explicitly reported ownership-preserving restart, not an unannounced process replacement. Keep the full-reload gate open until a stable run passes. This is new diagnostic evidence, not a delivered watcher fix or user acceptance.

## 13 September — joined Order-plan review, save and installer-document use

Packages 4/6 now initialise order_installation_plans through normal schema startup and include its zero-count check in disposable provisioning. Existing local-development-only document routes expose proposal review, explicit reviewed save and scoped read. The Order document panel presents scope/warnings and operational allowances, retains the review reason after failed saves, selects the saved immutable plan and prepares an installer draft from it. Preparation validates Client/Order/release/Position relationships and retains plan version/actor/reason in its source binding; it does not refresh or mutate sold costing. On reopen a saved plan can be used without reselecting the old Estimate scenario. Context choices remain bounded to twenty; older-plan discovery and a full saved-review-details UI remain gaps.

The first browser setup used an incorrect costing API path. After correcting it, the issued-costing immutability guard correctly rejected test creation of a scenario after release. The fixture now creates its saved calculation through the actual service before release, without disabling any guard. Run `c020a1c2-1fad-4a07-8d09-f4f3d8848ae7` exits 0 through customer acceptance, Order entry, plan preview, forced save failure/retained input/retry, one retained plan, installer PDF containing every accepted reference and unchanged saved costing JSON. Exact owned browser/profile counts are zero and user API remains unchanged during that run.

The two strengthened full-reload runs (latest `4d8004ab-9c87-4759-b075-87cb545af18a`) completed the functional reload → saved-plan selection → PDF step and cleaned their exact browser/profile resources, but exited 1 because the user's watched API restarted during acceptance. They are not full acceptance passes. The user-owned supervisor was not stopped/replaced. Source file timestamps do not show a contemporaneous server/shared edit; the restart cause is not established. Do not repeatedly rerun or weaken this gate. Repeat it once the watched-runtime cause is understood or stable. Prior stable save-to-PDF evidence remains valid.

Full application typecheck and four actual-schema/source/persistence checks pass. Remaining user acceptance, complete operational pack content, reviewed delivery and full-reload infrastructure verification stay open. Next bounded work: investigate watched-runtime interference read-only, finish saved-plan review/reopen feedback and stale-source browser coverage, then continue pack content/attachments/delivery. Existing survey-delivery-installation is updated without count/completion promotion. New schema startup adds structure only; no live business record, issued evidence, storage upload or live email was changed.

## 13 September — reviewed Order-plan persistence boundary

Package 6 now has a backend-only persistent Order-plan store with owned SQLite connections/transactions. Preview exposes only the safe document, scope changes and review warnings. Save requires explicit review, a reason and the exact preview fingerprint; source is reloaded inside the write transaction and changed evidence fails closed. Immutable numbered versions retain the operational proposal and its provenance, actor and reason. Request identities reuse completed saves across retries/restarts; reads require exact Client/Order/Estimate scope. The store does not edit sold Project Costing or issued evidence.

Three proposal/storage tests and full application typecheck pass. The storage fixture exercises concurrent duplicate saves, new service connections, stale preview, conflicting retry choices, cross-Order read denial, immutable update/delete guards, injected insert failure/rollback and successful retry, plus preserved earlier versions. It deliberately injects a proposal loader into a disposable relational fixture: this is persistence-layer evidence, not actual-source/API/browser acceptance. No application startup or live database schema was changed, and no normal-route integration is claimed.

Continuation: initialise the schema through governed startup and the clean disposable inventory; expose preview/save/read behind the existing local staff boundary; connect the Order review UI with retained input/failure feedback; allow installer-document preparation to select the immutable saved plan, checking exact source relationships. Then run actual-schema normal Order review → save → prepare → reload, including stale-source and failed-save recovery. Existing survey-delivery-installation remains partial and unaccepted. No new permanent rule is needed. User API ownership/state remains unchanged; no live emails, storage uploads or business-data mutations occurred.

## 13 September — Order operational calculation proposal boundary

Package 6 now has an internal deterministic Order installation proposal builder. It requires an exact retained release and matching saved scenario revision, enabled installation and retained rules; each accepted Position must match exactly one saved product row with unchanged quantity/dimensions. The proposed operational scope explicitly activates accepted historically excluded rows and removes non-accepted rows, records both changes, and recalculates with the existing installation engine and saved rule/profile/team evidence. It never edits the sold scenario or clones its customer pricing. Source and proposal fingerprints support the upcoming reviewed persistence boundary.

Two focused proposal tests and the existing three scope tests pass: accepted alternative inclusion, non-accepted exclusion, costed crew of three without incrementing to four, derived cill quantity, deterministic fingerprints, no customer-pricing projection, unchanged source, and fail-closed missing/ambiguous/different-size/disabled inputs. This is an internal service foundation, not a saved Order programme or normal-browser acceptance. No API/UI path calls it yet, and no new browser run is claimed. It does not complete the Ecoglaze policy, travel or complete pack requirements.

Continuation is precise: add an explicit review/save boundary that freezes this operational proposal, its source fingerprints and actor/reason under the canonical Order; reject changed source at submission, preserve retries/history, then allow the installer document to consume that saved Order calculation. Use disposable normal-route review → save → prepare → reload checks, including changed-source and failed-save recovery. Keep sold costing unchanged. Existing survey-delivery-installation remains partial/unaccepted; no new requirement or permanent-rule duplication is needed.

## 13 September — read-only installation calculation scope review

Package 6 now offers Check calculation against schedule before draft preparation. It reads the exact selected revision and saved scenario, explains missing/repeated/extra Positions and quantity/dimension differences behind View Position details, and returns an actionable next step. It does not expose commercial prices, modify calculations or create a document. Changed scenario/source identities fail review; preparation independently retains its existing strict validation. Changing document choices clears the displayed review.

Three focused scope/coverage tests pass, including historical accepted flags, wrong identity, difference values, source preservation and price omission. Full application typecheck passes. Normal fresh-database Files run `0d7bc09f-8383-474c-bdf0-3152da320788` exits 0 after the new matched-schedule review, preparation failure/retry, installer/client PDFs and eleven-document history recovery. Exact browser/profile remnants are zero; user API `18440e0f-d291-4c89-a73a-3dc939fe6a6e` is unchanged during browser acceptance. Mismatch presentation has service-level evidence; the complete Order mismatch-to-new-calculation browser journey is not yet delivered or accepted.

Existing survey-delivery-installation remains partial. Next: an explicit retained Order-specific operational calculation for differing accepted scope, using saved rules/profile evidence and reviewed changes without repricing or editing the sold baseline. This review is diagnostic, not an automatic correction or a full installer-pack completion. No new permanent rule is needed; AGENTS already requires exact revision ownership, clear differences and immutable sold evidence. No live emails or business-data mutations occurred.

## 13 September — accepted Position authority in installation documents

Packages 4/6 exposed a genuine omission while tracing Order calculation scope: the source loader retained exact accepted Positions, but projection and calculation coverage reapplied historical Estimate alternative/exclusion flags. An accepted alternative could disappear from the PDF, and coverage validation could overlook it. Order projection and coverage now respect the already-selected accepted source set; ordinary Estimate filtering is unchanged. Missing or excluded costing coverage still fails for review, and no source flags, sold costing or issued evidence are rewritten.

The existing disposable acceptance journey actually accepts W01, D01 and D01 ALT. Earlier checks proved the source binding but did not compare the rendered projection to that complete set, so their claims did not prove complete accepted schedule output. The strengthened normal-browser gate now checks every accepted reference in downloaded PDF text and exact accepted IDs in both saved projection and source binding. Final run `95d5ed1d-54d6-45f1-a1a3-2ac56c231f58` exits 0; PDF SHA-256 `37519611b424bdd28b23dd0d4f179a021ef5db31a45013ea5ec1fd3b0bce05bc` visibly contains all three references. Exact browser/profile remnants are zero; user API instance `28f6e2ed-fe9b-46ec-92ed-c91445cb38ae` is unchanged throughout the final run. This does not approve mutually exclusive customer-choice policy; it proves downstream fidelity to retained acceptance rather than silently overriding it.

Focused tests also inspect both PDF audiences for an accepted historically excluded alternative, reject missing costing coverage and prove unchanged source flags. The full application typecheck passes. Existing survey-delivery-installation evidence is updated without changing Roadmap counts or user acceptance. Continuation: implement explicit review and retained Order-specific operational calculation where accepted scope differs, reusing saved rule/profile evidence without changing the sold baseline; then complete operational pack contents and reviewed attachment/delivery. Existing prepared documents remain immutable, not regenerated. No live emails or business-data mutations occurred.

## 13 September — canonical Orders entry and full application typecheck correction

Packages 4/6 now provide an Accepted Orders view in the normal Orders sidebar. The previous Estimate-outcome view remains separately available as Estimates marked as Order; acceptance does not rewrite that legacy outcome to manufacture list visibility. The new read-only canonical query uses accepted Order IDs, Client/Project context, literal search and twenty-item pages. Opening an item reuses the existing staff Order journey and exact accepted-revision installation documents. Order load errors now expose a retry instead of an indefinite loading message. The new directory is local-development-only pending production staff authentication; it is not tenant-isolation evidence.

Disposable normal-browser run `b46dcad5-6d2a-4164-b1aa-fa31a3415538` exits 0 through customer acceptance → Orders sidebar → preserved legacy view → accepted Order → forced load failure/retry → locked retained-revision client schedule/PDF. Canonical accepted Position IDs, Order/revision/PDF linkage and zero factory requests pass. Exact browser/profile remnants are zero and user API `808c11b8-5f4c-4894-bc5d-940d2622244d` remains unchanged during acceptance; its watched supervisor is preserved. Two focused SQL/access tests cover twenty-two accepted records across bounded pages, literal wildcard search, inconsistent Project ownership, invalid inputs and untrusted-origin denial. These are query-layer fixtures, not an additional twenty-two-case browser journey.

Validation correction: earlier plain `npx tsc --noEmit` reports did not traverse this repository's project references and therefore were not full application typecheck evidence. The proper `npm run typecheck` (`tsc -b --noEmit`) exposed missing declarations for the recently introduced qualification date/summary JavaScript helpers. Added accurate declaration contracts; the full check now passes. Earlier browser/runtime checks remain valid, but the earlier narrow compiler claims must not be reused as application typecheck proof.

Existing canonical-order and survey-delivery-installation entries are updated, with no completion/count/user-acceptance promotion. Next: accepted-Order installation-calculation scope, missing operational pack content and reviewed attachments/delivery. No live communications, business record changes, order-size changes or issued-document rewrites occurred. No new permanent rule is necessary; AGENTS already requires correct validation depth and shared canonical workflow entry.

## 13 September — exact bounded document choices and history recovery

Package 6 now retains the exact open Order or selected saved calculation even when older than the twenty recent choices. A scoped lookup replaces one recent choice, keeping each response at twenty; unavailable or unrelated identities fail explicitly rather than falling back. This does not yet provide browsing/search for all older choices. Document history has immediate loading feedback, duplicate page-submit protection, a retained previous page after failure and a clear retry instruction.

An actual fresh-schema test creates twenty-one disposable Orders/calculations and proves bounded older selection, unrelated calculation denial and malformed selection rejection. Normal-browser run `329e0bee-173c-4b31-9428-02edd96bbcdd` exits 0 after preparation failure/retry, installer/client PDF download, eleven real prepared PDFs, history failure/retained page/retry/previous page and reachable 960×600 controls. Additional history was seeded through the production preparation API in the owned disposable database, not through live writes. Both exact owned browser/profile counts are zero. The user's watched API restarted automatically after the route edit and remained at instance `a1ba7a3c-0d8b-4615-93bc-09a28fdd2a89` throughout browser acceptance; supervisor ownership is unchanged. No emails were sent.

Existing `survey-delivery-installation` evidence and next action are updated without changing counts or user acceptance. AGENTS already supplies the ownership, bounded loading and recovery rules; no new permanent rule is needed. Continuation: accepted-Order installation-calculation scope and direct Orders entry, followed by complete operational contents, reviewed drawings/RAMS/certificates and delivery. Do not alter the sold baseline to make a mismatched calculation pass.

## 13 September — accepted Order document entry and retained site address

Package 6 now exposes the shared installation-document panel in the staff Order journey, using the canonical source Estimate ID and locking the selected Order. If that Order is absent from available choices, preparation fails instead of falling back to an editable Estimate. The new normal-route gate performs customer Position acceptance, opens the canonical Order through staff Portal, prepares its client price-free draft and verifies persisted accepted Position IDs, release/Order/revision linkage and PDF hash. No factory request is created.

PDF inspection exposed a real omission: the loader checked only the customer projection's siteAddress, while the released Estimate snapshot retains projectAddress. It now reads that retained snapshot when needed, never the current amended address. Focused coverage confirms the retained address wins over a changed working-site value. Existing prepared and issued PDFs are unchanged.

The first browser attempt had an incorrect button-only sidebar wait; the established navigation pattern corrected it. A subsequent functional pass failed its final user-API-instance gate after a watched restart and is not counted as full acceptance. Final run `2edba459-fd4f-4798-8e6d-ea34720865ea` exits 0 with the retained address in the downloaded PDF, zero owned browser/profile remnants and unchanged user API `c4451513-6827-41c2-a2d8-1d94ecdc0e82`. Seven focused installation-document tests and TypeScript pass before the address correction; its source regression and TypeScript pass again afterwards. No live business records, emails or order sizes were changed.

Existing `survey-delivery-installation` implementation/evidence/next action is updated without status/count or user-acceptance promotion. This proves the staff Order client-schedule path, not Order installer-costing or the complete installation pack. Next: direct Orders-list entry and explicit calculation scope for exactly accepted Positions, then reviewed drawings/RAMS/certificate attachments and Email delivery alongside remaining operational content. Preserve the immutable accepted baseline; do not silently reuse an installation calculation containing unaccepted alternatives. Ordinary-user acceptance and production staff/tenant security remain open. AGENTS already covers these rules; no duplicate amendment is needed.

## 13 September — normal Estimate document preparation/reopen integration

Package 6's prepared-document schema is now initialised by the application, and local-development routes expose exact Estimate context, ten-document history pages, preparation and integrity-checked scoped PDF download. Files / Documents contains separate Installer pack draft and Client schedule without prices choices, explicit source/calculation selections, immediate preparation feedback, retained choices after errors and reopenable prepared-not-sent history. No send command exists in this panel. The application currently supplies a development staff identity, not production authentication: these new routes fail closed in production/non-loopback access and reject untrusted browser origins. Focused denial checks execute before database access; they are not evidence of full tenant isolation.

The new normal-browser route exposed two defects missed by isolated fixtures: canonical working Positions store qty while released projections use quantity, and opening Files before costing finishes can leave an empty choice list. Source selection now normalises the evidenced qty field without replacing explicitly missing quantity. The panel refreshes when its saved scenario identity arrives, preserves deliberate selection and ignores stale load responses; empty choices also offer refresh. The actual-schema source fixture now uses canonical qty.

Final normal run `762774e6-9816-4576-9eab-c0cbb2285c7b` exits 0 through forced preparation failure → retained choices/retry → installer draft download → close/reopen history → separate client price-free PDF → reachable action at 960×600. Two canonical prepared records and exact PDF hashes are checked. PDF text excludes installer allowances from client output and visual inspection confirms readable explicit unknowns. Owned browser/profile counts are zero; test ports 3104/5276/9416 are absent. The user's watched API supervisor PID 29604 remains; child PID 22844/runtime instance `538c29e7-781f-4dfd-8dc2-5579cadaf406` is unchanged throughout final acceptance. A read-only nonexistent-ID endpoint probe confirms the new runtime route. Earlier failed runs also cleaned their owned resources and are not counted as acceptance.

Seven focused source/calculation/projection/store/access tests, TypeScript and 28 Roadmap checks pass. The isolated initializer now explicitly verifies zero installation_prepared_documents before seeding. No live document, communication, commercial record or issued evidence was changed; normal watched schema startup added only the new empty structure. Existing `survey-delivery-installation` is updated without status/count or ordinary-user acceptance promotion. AGENTS already governs these boundaries; no duplicate permanent rule is needed.

Next: complete positive accepted Order source and normal Order entry, then reviewed attachment selection and Email handoff alongside the remaining weights/materials/crew/travel/survey content. Current context choices are bounded to twenty recent scenarios/Orders: older-selection support, history pagination/race recovery and full themes/zoom remain review gaps. This panel is explicitly a draft foundation, not the complete requested installer pack. Production staff/tenant authorization and abrupt-process orphan reconciliation remain release/depth gates.

## 13 September — retained preparation and scoped generated-PDF reads

Package 6 now has an explicit prepared-document schema/store. A dedicated SQLite connection owns each immediate write transaction across exact source/calculation selection, PDF generation, exclusive managed-file creation, fsync, metadata and commit. Request identity provides concurrent/retry protection; changing reviewed request choices under the same key is rejected. Prepared metadata is immutable and the returned state is prepared_not_sent. Client/Estimate-scoped reads verify both retained byte length and SHA-256 and expose the safe projection, not staff-only source-binding data. A confirmed prior preparation is reused even after later schedule edits; it is not regenerated or implicitly described as current/sent.

The actual isolated application schema test passes two concurrent requests yielding one identity, reopened-service reuse, scoped download denial, immutable metadata, failed-insert rollback and cleanup of only the newly owned generated PDF, corrupted-file refusal and preservation after a later working-schedule edit. Six focused source/calculation/projection/PDF tests and TypeScript pass. A test initially omitted the required Estimate scope on download; adding that caller scope confirmed the fail-closed read contract. Persistence failures now provide retry/retained-choice wording while preserving their technical cause for diagnostics.

The new schema is explicitly initialised only by the disposable test at this checkpoint; it is not yet wired into application startup/routes. No live API was controlled, no live/provider records were written, and no email was sent. Abrupt-process orphan-file reconciliation and positive real-schema accepted Order preparation remain unverified; ordinary exception/rollback checks are not evidence of a full crash-recovery gate. Installer output is still a review-required draft with remaining content gaps.

Existing `survey-delivery-installation` is updated without status/count or user-acceptance promotion. Next: wire the schema/internal store behind the established staff/runtime boundary, add bounded list and scoped download/prepare routes, then expose normal Estimate/Order preparation/reopen feedback. Follow with canonical Files/RAMS/certificate/drawing selection and reviewed delivery; keep final-send freshness checks separate from reopening retained evidence. Preserve all remaining weights/materials/travel/survey and customer-provisioning gates.

## 13 September — selected saved-costing binding for document preparation

Package 6 now joins exact revision source selection to the existing canonical scenario read service. Preparation requires an explicit scenario ID/revision belonging to the selected Estimate, never selects the latest scenario implicitly, validates complete included canonical Position identity/quantity/dimension coverage, and requires the saved installation-programme rule snapshot. Missing/duplicate mappings, stale revisions or changed dimensions fail with a review instruction. Alternatives are excluded consistently. The loader rechecks source/scenario revisions after reading; persistence must still enforce its own atomic freshness boundary. Staff-only source/calculation fingerprints are separate from the safe document projection.

Six focused source/calculation/projection/PDF tests pass. In the real isolated application schema, the test creates a normal Estimate-origin scenario through the production service, then enables SQLite query_only and loads the prepared installer projection through both real services. Stale scenario selection is rejected. Disabled installation yields unconfirmed days; no current Administration default is substituted for a missing rule snapshot. Mapping/quantity/dimension failures and changed operational fingerprints also pass focused unit checks. TypeScript passes. No API, browser or live provider was used; these are service checks, not normal-route pack acceptance.

Existing `survey-delivery-installation` evidence is updated without status/count or user-acceptance promotion. Next: immutable document preparation metadata/bytes, scoped download and retry/partial-failure protection, then Estimate/Order normal-route integration and reviewed attachment/delivery. Actual accepted Order integration, complete installer contents, travel-return policy and survey workflow remain open. Do not expose staff-only source objects through customer or installer routes.

## 13 September — read-only installation document revision authority

Package 6 now has `loadInstallationDocumentRevision`, selecting a working Estimate only when its requested revision still matches, or using the immutable release projection where available. Order selection follows its exact acceptance/release and includes only Position acceptances explicitly marked accepted. Order, release, Client, Project and Estimate identities must agree; missing/duplicate Position identities fail closed. An issued revision without a retained release projection is blocked rather than silently substituted with today's editable schedule. Missing historical site-address evidence stays unknown. This loader performs no file or database mutations and is not yet exposed through a user-facing route.

Five focused source/projection/PDF tests pass. The working-revision/stale-selection test initialises the actual application schema in a new disposable database, verifies no copied live data, and runs source reads with SQLite query_only enabled. Order retained-Position selection/conflicting-Client checks are a separate mocked-selection unit test, not positive real-schema Order or browser evidence. TypeScript passes. No API was started, restarted or controlled, and no live records/provider state were accessed.

Existing `survey-delivery-installation` implementation/evidence is updated without status or user-acceptance promotion. Next: join this exact source to the selected persisted scenario/rule snapshot with complete canonical Position/quantity/dimension coverage, then retain immutable prepared document metadata/bytes with duplicate and partial-failure protection. Add actual-schema issued/accepted Order coverage and normal Estimate/Order prepare/download before joining reviewed Files/RAMS/certificates/Email. Document persistence and delivery remain unimplemented; this source-selection checkpoint must not be described as a complete installation workflow.

## 13 September — separate installer/client document projection foundation

Package 6 has started the missing installer-pack implementation. Inspection found the saved costing service already returns a snapshotted-rule installation programme with operational allowances and retained route identities, while RAMS source context lacks most pack fields. New `installationDocumentProjection.js` deliberately allowlists selected revision Positions and separate client/installer output rather than spreading commercial costing objects. Client output omits installer allowances entirely. Installer output includes crew, days, food/accommodation/cill allowances with their units, inclusion decisions, safe qualification summary and retained outward-route evidence. Windows, doors, sliding doors, lift-and-slide and bifolds have separate totals; unknown categories stay explicit. Missing data stays null/Not confirmed. No return-route time is invented by doubling an outward route; return-route resolution/manual policy remains to implement.

The separate PDF renderer currently emits a clearly labelled review-required draft only. Three focused tests pass using the real installation programme return shape, wrong-Estimate and duplicate-Position rejection, source preservation, both PDF byte streams and independent extracted-text checks for audience separation. TypeScript passes. This is pure projection/rendering verification, NOT a persisted API or browser pack workflow, and it does not establish complete price/content acceptance. No API was started/restarted or live business/provider data accessed. Existing issued files remain untouched.

Existing `survey-delivery-installation` is updated without status/count or user-acceptance promotion; AGENTS already governs audience separation, missing evidence and revision authority. Exact continuation: bind the projection to read-only persisted Estimate/accepted Order revision authority and saved scenario/rule/material snapshots, validate complete canonical Position coverage, then add immutable preparation/download and normal Estimate/Order actions using the existing reviewed Email pipeline. Remaining required content includes weight evidence/fallback, material quantities, selected drawings and RAMS/certificate attachments, full travel return/pattern/cost policy, installer-specific crew policy and survey/threshold/Purenit approval. Do not expose this draft renderer as a completed installer pack or silently send it.

## 13 September — concise renewal-aware summary and issued-review reload

Package 6's safe qualification summary now collapses only explicit same-type renewal chains. It preserves unrelated cards, retains earlier verified evidence when a future/unverified renewal cannot cover attendance, and retains malformed cyclic lineage for review. No qualification rows, retained source snapshots or previously issued PDF bytes are modified. Validity and evidence verification remain independent; certificate identifiers, filenames and private notes stay excluded.

Eleven focused date/summary/provider/foundation tests, TypeScript and 28 Roadmap checks pass. The normal qualified-team RAMS browser gate now performs a full page reload after issue, reopens the exact Estimate's issued review, downloads the PDF and creates a separate editable successor. Run `92520bf9-83c1-4f1d-a139-dd5236737ede` exits 0; both browser/profile remnants are zero, and user API `40986ac3-737a-4ffb-b572-351bb2ca23af` remains unchanged during acceptance. PDF visual inspection confirms one relevant CSCS entry per test installer with readable independent verification wording. The first reload test failed only because its open condition expected the draft-only summary; it now recognises the issued panel too. That failed run also completed owned cleanup.

Existing `survey-delivery-installation-basic-rams` evidence/next action is updated; no Roadmap status or user acceptance is promoted. AGENTS already governs immutable history and concise safe projection; no duplicate rule is needed. Next bounded implementation is the missing installer-pack generation/review/delivery from saved installation values and exact revision identity, reusing existing safe schedules, Files and reviewed Email. Include the Order route, selected certificate copies and RAMS handoff; do not treat the current RAMS-only gate as delivery of that pack. Broader workforce themes/zoom, uncertain-upload reconciliation, briefing UI and ordinary-user acceptance remain open.

## 13 September — normal qualified-team to RAMS integration gate

Package 6 now has a joined normal-browser gate: `node scripts/run-complete-customer-order-journey-browser.mjs --stop-after-qualified-team-rams --require-existing-user-api`. It first exercises the existing Administration upload/replacement/renewal/failure-retry journey, then creates two explicitly disposable, separately evidenced installers and a two-person team through Administration. The normal Estimates list opens the working Estimate directly; the test selects that Company/Team in Installation and continues through Files / Documents into RAMS without clearing the retained required-CSCS choice.

The passing run verifies competent-review blocking, saved attendance/requirements on reopen, reachable final actions at two desktop sizes, the issued qualification disclosure, two persisted verified/in-date CSCS members, the downloaded two-page PDF and an editable successor preserving issued evidence. PDF text checks exclude certificate identifiers and evidence filenames; visual inspection confirms readable pages. The summary currently includes earlier renewal-history rows alongside current evidence: concise current-summary selection remains a bounded UX gap, not lost history. Full browser refresh of the issued disclosure, the Order route and installer-pack generation/review/delivery remain outstanding.

The first joined attempt failed because the test incorrectly expected a second Open action after the normal Estimates list had already opened the working Estimate. Correcting that navigation assumption produced exit 0, run `7ac3bd86-aa4b-41b4-a152-c411695c65e3`, with exact owned browser/process/profile counts zero and unchanged user API instance `d49a28a3-3210-4c94-8221-0d0452da66e1`. Fresh isolated schema, test-only evidence and the no-network provider transport were used; no live communications, provider writes or business-record changes occurred. The runner's output now distinguishes the missing-team regression branch from this qualified-team issue branch rather than claiming both checks ran together.

Ten focused qualification/date/provider/foundation tests, 28 Roadmap checks and TypeScript pass. Independent post-run inspection confirms ports 3104/5276/9416 absent and only the unchanged user API on 3001, PID 42828 under watched parent 29604.

Existing `workforce-domain`, `workforce-domain-1` and `survey-delivery-installation-basic-rams` verification evidence is updated. No status/count or user-acceptance promotion; the original 73 mapping is unchanged. Permanent rules already cover this work, so AGENTS needs no duplicate change. Next: concise current qualification summary and reopened issued review, then the missing installer-pack projection/PDF/reviewed delivery from saved installation data and exact revisions. Preserve unrelated ConfigureInstallation edits.

## 13 September — retained RAMS workforce check and safe qualification projection

Package 6 handoff inspection confirms installer-pack generation is not implemented: the current code only contains an installer_pack qualification snapshot kind, not the requested prepare/review/PDF/delivery workflow. Do not describe this as merely an attachment-picker omission. Its document generation and reviewed delivery remain authorised work under the existing installation package.

RAMS previously calculated a qualification summary for the PDF without retaining the structured issue check, and showed a raw validity status without evidence-verification status. It now freezes the attendance/team/member/qualification check in the issued source snapshot, uses a shared customer/installer-safe summary that omits certificate numbers, file IDs and private notes, and exposes the retained check in an optional issued-review disclosure. Validity and evidence verification are explicit independent wording. A selected team's membership, qualification evidence or active-state changes contribute to the source fingerprint and require review without rewriting issued evidence. Legacy source hashes are compared using their original field basis; missing historical workforce-review evidence is explained rather than falsely described as a source change. Selected teams require both attendance dates, and malformed qualification-choice input is rejected rather than cleared.

Eight focused date/summary/foundation tests and TypeScript pass, including persisted issue check, retained history after staff changes, summary privacy and malformed requirement rejection. The existing normal Estimate → Files / Documents → RAMS failure/recovery → issue/PDF → successor regression exits 0: run `00ab407d-0762-4217-8bc5-04be67d78ba4`, exact browser/profile remnants zero, user API instance `c55db239-5774-465b-a6f1-ae55defb3049` preserved. This existing browser gate exercises missing-team qualification blocking and explicit test-scope requirement review; it does NOT prove the fully qualified-team selection, new issued-team disclosure, or installer-pack handoff. Those remain separate required gates.

Existing `survey-delivery-installation-basic-rams` advances implementation/service verification without original-73 status or ordinary-user acceptance promotion. AGENTS already governs immutable workforce/RAMS evidence and safe document projections, so no duplicate rule is needed. Exact continuation: create/select the attending team through normal Administration/Estimate Installation, use the previously verified provider-backed qualification flow, verify missing/unverified versus reviewed evidence through RAMS issue, and inspect the saved check and safe PDF summary after refresh. Then implement the missing installer-pack projection/PDF/reviewed delivery using saved installation calculations and canonical revision/file identities. Preserve the unrelated ConfigureInstallation.tsx changes and do not send live communications.

## 13 September — replacement evidence and calendar-date boundaries

Package 6 now uses one shared qualification-date calculation in Administration and the server. The old end-of-day arithmetic delayed the 30-day Expiring warning by one calendar day; exactly 30 days now qualifies, the expiry date remains valid, the following day is expired, and valid-from is inclusive. Missing/invalid dates remain explicit and invalid date saves/attendance requests fail before mutation. Qualification checks also refuse to describe an inactive Company/Team or a Team with no active installers as available.

The installer form exposes Previous evidence from retained canonical upload history, separately from renewal history. The normal disposable Administration journey replaces a PDF with changed bytes under the same filename, reopens it, checks Previous evidence, exercises six planned-attendance boundary dates, then completes renewal plus later-validation-failure/retry. It verifies two distinct checksums, two retained upload/history rows with one current file, and only four qualification records. Run `be76075c-1cba-4dfb-bfa0-f16b99cf5020` exits 0 with exact browser/profile cleanup and unchanged user API `39f432e6-7574-4e90-942e-d2c20f060204`. No live delivery/upload or genuine certification claim is made.

Nine focused date/foundation/provider tests pass, including impossible/leap dates, precise warning/expiry boundaries, inactive/empty-team blocking, receipt recovery and preserved RAMS. TypeScript passes. This advances existing `workforce-domain` and `workforce-domain-1` without changing original-73 statuses or user acceptance. AGENTS already governs retained replacement/renewal evidence and independent validity/verification, so no additional duplicate permanent rule is needed.

Next exact continuation: join the attendance-qualified team to the normal Estimate/Order installation selection, RAMS issue/snapshot and reviewed installer pack. Verify team/qualification failure recovery through the normal route, qualification summary privacy and explicitly selected certificate copies. Remaining before full workforce acceptance: broader current/Legacy theme and zoom coverage, real restart/uncertain-upload reconciliation UI, historical orphan review and ordinary-user acceptance. User isolated checklist: Edit Installer → replace evidence using the same filename → save/reopen Previous evidence → check a date before valid-from, exactly 30 days before expiry, expiry day and the next day → confirm evidence verification is still separate.

## 13 September — installer qualification uploads, renewal recovery and modal access

Package 6 normal-browser inspection found three concrete blockers: qualification uploads supplied 21 SQL values for 20 document columns; the canonical document constraint had no installer ownership relationship; and the modal was rendered inside a lower workspace stacking context, allowing Programme & Rates to cover it while errors remained behind it. Separately, retries discarded newly saved qualification IDs and repeated renewal insertion. These are implementation defects, not user-accepted behaviour.

The Add/Edit Installer form now retains stable qualification/renewal IDs and each confirmed upload result, refreshes its final persisted state, prevents concurrent saves, preserves uploaded evidence against stale form retries and shows feedback within a top-level modal. Renewal updates retain their lineage and cannot change installer/type ownership. Canonical documents gain a real installer foreign-key relationship through a transactional, fail-closed migration invoked before the first qualification upload, without fabricated commercial relationships. Existing columns, rows, indexes and triggers are copied unchanged; injected post-copy failure rolls back. This migration was exercised only on disposable databases, not by uploading into Ecofenster.

The provider boundary persists a unique qualification/account/folder/name/content upload claim before IO and a confirmed receipt before local linking. Local failure can reuse the exact provider identity, checking its current folder and checksum. Uncertain provider outcomes remain blocked, including through a new service instance; no false success or automatic resend. Canonical evidence and renewal history remain metadata over provider-owned binaries. A complete staff reconciliation UI for uncertain/older orphaned uploads is still missing and must not be implied by the recovery warning.

Eight disposable foundation/provider tests pass, including migration preservation/rollback, exact confirmed-file recovery after an injected local failure, replay with one file/history row, stale-form preservation, cross-installer evidence rejection, uncertain-provider duplicate blocking and RAMS regressions. TypeScript passes. The final normal Administration → Installation → Add Company → Add Installer → blocked upload → retry → reopen evidence → Add renewal → later qualification validation failure → retry → retained history gate exits 0. Run `1f05feb1-44d1-48fb-9b5b-8e131c3fb6cb` uses real pointer clicks on unobscured Save controls and retains one installer, four qualification rows (three types plus one renewal) and one evidence upload. Exact browser/profile cleanup is zero and user API instance `a5cb68b3-e492-4222-8212-019e25a57374` is unchanged. The corrected modal screenshot was visually inspected. The provider transport is explicitly no-network and the uploaded PDF is labelled disposable, not a real certificate; no live emails, uploads or business-data copies were used.

Existing `workforce-domain` and `workforce-domain-1` advance implementation/technical evidence only; no original-73 status or user acceptance is promoted. Remaining: same-qualification replacement/history, full date-boundary/browser theme/zoom checks, actual restart and uncertain-upload reconciliation UI, historical orphan review, and the attendance-qualified team → RAMS → reviewed installer pack join. No claim of live Drive or full competence/permission acceptance. AGENTS now records installer document ownership and receipt-safe qualification retries.

Exact continuation: complete replacement and date/attendance boundary checks in the same isolated Administration journey, then select the verified team in installation and verify RAMS qualification gating/snapshot and installer-pack attachment selection. User checklist (isolated workspace only): Add/Edit Installer → enter dates and select test evidence → save/reopen → check validity separately from evidence verification → add renewal → verify original evidence remains in history → confirm both Save and Cancel stay reachable. Uncertain uploads must not be retried under a new filename merely to bypass the retained claim.

## 13 September — normal-route Basic RAMS review and retained attendance

Package 6 now exposes the existing Basic RAMS editor in the normal Estimate Files / Documents dialog. Saved attendance start/end and required qualification types restore on reopen and successor revision; issue checks the saved requirement rather than relying on repeated transient fields. Input handlers capture values before React deferred updates, initial loading no longer races draft creation/hydration, and immediate in-flight guards prevent repeated commands. The surrounding dialog owns scrolling instead of a second RAMS scroll area. Issued PDF links use the configured API origin. These changes preserve historical issued documents.

The fresh-database `--stop-after-rams-review --require-existing-user-api` normal-browser gate passes failed-save/retained-entry recovery, nine reviewed hazard categories, attendance and CSCS requirement save/reopen, missing competent-person and missing-team qualification blocking, explicit test-scope requirement review, PDF issue/download and immutable R1 plus editable R2 persistence. Final actions are reachable at 1440×900 and 960×600 with no page overflow. The downloaded two-page PDF contains the saved dates and named reviewer, and its hash matches persisted evidence: `dd2d3b34f70603c33bc8203a55ac7131fa31954f1214655829836157a81cbf42`. The rendered PDF and compact dialog were visually inspected. Four foundation tests and TypeScript pass.

An earlier functional run failed the final user-API identity gate and is not counted as full acceptance. The stable rerun exits 0, verifies zero exact owned browser/profile remnants, removes only its fresh test infrastructure and retains user API instance `32d3077a-c6f6-4713-85de-4491e52ce223`. No live database copy, provider credentials, filing or email delivery is used. Test hazards/reviewer wording are disposable exercise inputs, not a real competent-person assessment.

Existing `survey-delivery-installation-basic-rams` advances technical evidence only. The original 73 mapping and status totals do not change. AGENTS already covers retained competence evidence, guided recovery and immutable RAMS; no duplicate permanent rule is needed. Not yet verified/delivered: normal Order entry, installer qualification provider-upload/renewal, supporting-file and installer-email-pack handoff, briefing UI, theme/zoom coverage beyond these viewport checks, and stricter handling of missing attendance/team information. Unknown operational fields must remain explicit; this test is not a claim that the complete installation/RAMS programme is ready or user accepted.

Next exact continuation: normal Administration Add/Edit Installer qualification upload, failed-upload recovery and renewal with the no-network disposable provider transport; then join its attendance-qualified team to RAMS and the reviewed installer pack. User checklist in the isolated workspace: Estimate → Files / Documents → Create / review RAMS → review work/hazards → save dates/requirements → close/reopen → confirm responsible-person review → open issued PDF → create a new revision and confirm the original remains unchanged. Do not use this test as authorisation to issue live RAMS.

## 13 September — canonical Order gate and RAMS attendance persistence

The existing `--stop-after-source-backed-customer-order --require-existing-user-api` normal-browser gate passes after the customer canonical-ID repair: the exact genuine-source successor PDF opens in Portal, partial Position acceptance remains blocked, all five canonical Positions create one Order pending staff approval, refresh preserves that Order and no factory delivery occurs. Run `390aad11-5d6b-439e-a0a1-a5635653f949` verifies zero owned browser/profile remnants and preserves user API instance `5106d5fe-97ef-4f57-98e8-ded5f199083c`. This closes the recorded downstream identity regression check, not ordinary-user or public Portal acceptance.

Package 6 inspection found saved RAMS discarded attendance end/required qualification types; the UI reset end to start and requirements to empty on hydrate, while issue used transient request fields. The bounded repair retains reviewed attendance in the immutable version source snapshot, restores it on reopen/revision, validates real and ordered dates before writes, checks saved requirements when issue omits those fields, and prints the reviewed date range. Historical issued records remain unchanged. Four disposable Service/installation tests pass, including omitted-field preservation, missing-required-qualification issue blocking, rejected dates without mutation, revision retention and PDF text verification; TypeScript passes. These are service/PDF checks, not normal-browser RAMS acceptance.

Exact continuation: build the Package 6 normal-route browser journey using `initializeIsolatedJourneyDatabase` and the owned-browser helpers, without copying `quotesync.db` or executing live cleanup. The untracked old installation-operations script copies the live database; the tracked stage6 script defaults to live database cleanup. Neither was run. Check normal Estimates/Orders reachability of RAMS (currently rendered in EstimateExpandedPanel), expose review of configured required qualification types where needed, then verify attendance save/reopen/failure recovery, issue/revision and installer qualification Add/Edit/upload/renewal. Keep this package open until those normal-route gates pass. No live business data or delivery was used.

## 13 September — joined two-supplier source review and customer reissue

The previously separate source-ownership and single-supplier gates are now joined through the normal application. `--stop-after-two-supplier-imports` proves that the exact filed EKO PDF and Zyle DOCX produce five plus twenty-one canonical supplier/costing rows after reload without changing the first supplier's row identities or amounts. `--stop-after-multi-source-customer-reissue --require-existing-user-api` then uses a disposable issued baseline whose 001 and W1 references, dimensions and quantities are explicitly matched to those real sources. The test-owned requested finishes match the actual EKO page 1 and Zyle W1 item 2 evidence; this is not a claim about the real customers' prior requirements. Both supplier reviews retain their earlier reference checks and add Position checks, overall fields select their own document, and additional EKO/Zyle Positions are reviewed separately before customer preparation.

This full route exposed a real downstream defect: customer projections used costing-row IDs although the source imports had correctly retained canonical Estimate Position IDs. New projections now use the linked canonical ID, preserve unmatched legacy identity and do not rewrite historical issued documents. A further visual check exposed Timber → Internal finish mislabeling; Timber is now an independent material concept, while Internal is retained only for explicit finish evidence. Both corrections have focused regression coverage; all 28 quotation tests and TypeScript pass.

The final normal-browser run `5a960679-8f4f-4d4f-9089-2e935a8b06c5` passes all 26 canonical IDs in the saved customer projection, both reviewed finishes, readable genuine drawings, an 18-page downloaded PDF, matching retained document/email/provider MIME bytes, injected confirmed pre-send failure, safe retry, exactly one successful no-network customer message, one successor release/follow-up and restored issued state after reload. Original issued PDF/Positions remain unchanged. Exact browser process/profile counts are zero. The required-existing user API remains instance `1ca13aae-017c-4cd1-ac37-51960d299cac`, watched child PID 35812 under the existing parent 29604. Earlier attempts that started during a watched restart failed the ownership gate and are not reported as full acceptance. The explicit readiness option now waits for the existing compatible healthy API before starting any test infrastructure; final identity equality is unchanged. Source copies and provider transport are disposable; no live OAuth, filing or delivery is used. The representative PDF page was visually inspected: EKO 001 and Zyle W1 retain their distinct dimensions/finishes and Timber: Softwood, not Internal: Softwood.

Updated existing entries: `manufacturer-rfq-lifecycle`, `manufacturer-rfq-lifecycle-8`, `quotation`; AGENTS records canonical customer identity and material/finish separation. Original 73 mapping and status totals are unchanged; no user acceptance is promoted. The joined return/import/review/reissue technical gap is closed for these sources, not all supplier formats, uncertain matching or live provider operations. Optional RFQ drawings, genuine returned factory confirmation, broader file browsing, EF-only provisioning and production Portal/tenant boundaries remain explicit.

Continuation: first run the existing controlled source-backed customer Order acceptance gate against the corrected canonical customer IDs, then resume the ready Package 6 normal-browser installer qualification/Basic RAMS workflow. User checklist: (1) reopen both prepared supplier requests and distinguish prepared from sent; (2) select/file each exact returned document and explicitly import it; (3) review each requested Position and additional scope against its own source; (4) review preview/PDF, particularly 001/W1 and material/finish labels; (5) in controlled test delivery only, confirm safe failed-send retry and the one issued successor after refresh. Ordinary-user acceptance, not developer guidance or test success, remains the final usability gate.

## 13 September — repeat review evidence correction before reissue

Tracing the multi-supplier review prerequisite found a persisted-evidence mismatch in `verifySupplierRevision`: repeated field checks derived status from newly submitted before/expected values, but the UPDATE retained old requested wording, before/expected values and before-source reference. A review could therefore report implemented while its saved expected value differed from its returned value. Both existing-row and conflict-update paths now save the complete reviewed field basis. Prior values remain in immutable review history; original Portal comments, issued Estimate and source documents are not changed.

All 22 lifecycle/Portal tests pass, including correction of staff transcription with history retention, unresolved blocking and subsequent reviewed recovery. The normal `--stop-after-source-review-correction` gate deliberately saves a mistaken expected value, verifies needs_review and no overall approval, then corrects it using the genuine EKO source and checks persisted expected value/source reference plus the earlier history. Combined with `--stop-after-source-backed-customer-reissue`, it passes the complete corrected-review → five-Position customer preview → reviewed terms → downloaded six-page PDF → controlled failed send/retry → one issued successor → reload path. Saved PDF and Email attachment bytes match; one release/follow-up and one successful no-network customer message are retained; original issued evidence is unchanged. The PDF schedule was visually inspected and shows the retained drawing and RAL 7016 finish legibly. Both browser runs verify exact process/profile cleanup and user API preservation. No live delivery occurred.

This repairs a required review/reissue dependency; it does not turn the separate two-supplier quotation-reference checks into proven Position compliance. Existing `manufacturer-rfq-lifecycle` and `manufacturer-rfq-lifecycle-8` are updated without original-73 status or user-acceptance promotion. AGENTS already requires immutable review/source evidence and explicit reissue, so no duplicate rule is needed. Next exact continuation: source-owned multi-supplier Position mapping and overall source review, then the joined multi-supplier customer reissue gate. Keep generic reference-only reviews distinct from requested Position-change verification.

Isolated user sequence: save an unresolved staff transcription → correct the reviewed values/source reference → reopen and inspect retained history → review customer preview/PDF → use controlled test delivery and check the issued successor. Do not edit live issued evidence or send live Email for acceptance.

## 13 September — real restart and incoming acknowledgement cancellation

The no-network provider fixture now persists its sent receipt projection and attachment bytes inside the uniquely owned temporary journey directory. Restoring it requires the same explicit test-recipient boundary; it contains no live credentials or copied business database. This is test-provider continuity, not a new application mailbox store. Focused round-trip tests verify exact receipt/attachment equality and reject another recipient allowlist.

`--stop-after-followup-restart` passes the normal workflow through an uncertain follow-up, stops only the owned API tree, starts a new API process against the same disposable SQLite database and integration settings, verifies changed runtime identity and preserved claim/receipt, then recovers through Check follow-up outcome. The successful run changed test PID 14596 → 18676 and retained exactly two provider messages. An earlier functionally successful run failed its final user-API baseline guard because the baseline was temporarily unavailable; it is not counted as a full pass. The stable rerun exits 0 with independent zero browser/profile remnants and unchanged user API instance.

`--stop-after-followup-cancellation` passes reviewed initial sending → exact incoming message → Link as acknowledgement → reviewed expected-return date → actual owned API restart → observation beyond one 60-second worker interval. Only the original message was sent; no follow-up claim was created, the response remains acknowledgement_received and no revised-document filing is invented. Inspection exposed a real bug: changing expected return rewrote the parent to sent_to_supplier and repopulated a cancelled follow-up date. Deadline edits now preserve the response lifecycle and do not requeue a response-satisfied follow-up. The browser asserts supplier_reply_received, no pending follow-up and no document link; cleanup and user API preservation pass.

All 22 lifecycle/Portal tests pass, including that deadline regression; four receipt/persistence/guard tests pass. Existing RFQ Roadmap entries are updated without acceptance promotion. Permanent AGENTS recovery/process-ownership rules already cover this work, so no duplicate rule is added. Next bounded package: complete remaining supplier-document/change-review/reissue joins, starting with the pending genuine multi-supplier Position ownership and overall customer reissue evidence. Optional RFQ drawings, genuine returned factory confirmation and ordinary-user/live-provider acceptance remain explicitly open. Original-73 mapping and status counts are unchanged.

Isolated user checklist: (1) reopen a retained uncertain supplier follow-up and check its outcome; (2) link the exact acknowledgement without filing its attachment; (3) adjust the expected return date and confirm Reply received remains visible and the revised document is still outstanding; (4) restart the isolated workspace and confirm those results persist. Do not send live Email for this checklist.

## 13 September — automated follow-up receipt recovery

Tracked follow-ups now retain the connected account, exact receipt marker and reviewed-content fingerprint before provider IO, alongside their existing persisted claim. The supplier composer exposes Check follow-up outcome for uncertain or partial confirmed results. It reuses bounded sent-message/content verification, leaves missing evidence blocked, records actor/time and confirmed provider identity, and restores the retained communication without another send. Recovery preserves response/waiting state and deadlines. Conditional worker completion/failure writes prevent a late provider result from downgrading a concurrently confirmed receipt.

Normal `--stop-after-followup-reconcile` browser acceptance passes supplier preparation/send → reviewed due-date adjustment → actual scheduled worker → accepted follow-up with lost response → reopen → Check follow-up outcome → confirmed history. Exactly two no-network messages exist: one initial request and one follow-up. The response remains outstanding, confirmed follow-up evidence persists, and exact owned browser processes/profiles are absent; the user API instance is preserved. The initial log still printed uncertainRecoveryVerified:false despite executing and passing the conditional recovery assertions; the logging flag now accurately reflects that selected gate. This is not actual process-restart or live Gmail evidence. Service coverage also checks unconfirmed blocking, confirmed replay and preserving a previously recorded acknowledgement. The retry rejection assertion was corrected to the earlier applicable response-recorded guard, not by weakening the application safeguard.

Existing `manufacturer-rfq-lifecycle` and `manufacturer-rfq-lifecycle-4` advance in implementation/technical evidence only. The existing AGENTS delivery-recovery rule already applies, so no duplicate permanent rule is added. Next exact continuation: actual isolated API/database restart with persisted provider receipt state, then normal-route incoming acknowledgement/document cancellation and no repeat delivery. User acceptance and the broader supplier response/reissue workflow remain open; no original-73 status promotion.

## 13 September — supplier uncertain-delivery receipt recovery

Initial/revision supplier sends now persist a supplier-specific RFC Message-ID, reviewed-content fingerprint and connected account on the delivery claim before provider IO. The existing bounded exact SENT lookup and attachment-byte verifier are reused with a separate supplier marker; factory compatibility remains tested. Check delivery outcome verifies the exact retained message, account, recipients, subject/body and attachment hashes without sending. Missing, ambiguous, mismatched or older unmarked evidence leaves the claim blocked. Recovery retains actor/date, restores the communication and request projections, uses the original actual sent time for the seven-calendar-day deadline and does not reset an already reviewed deadline or regress completed/cancelled parent work. Late send failures cannot downgrade an already reconciled claim.

The normal `--stop-after-supplier-reconcile` browser gate passes genuine-source import/reload → reviewed supplier sending → provider accepts but response is lost → Check delivery outcome → exact receipt confirmation → reopen. Exactly one no-network provider message exists. Persisted child and parent states and deadline are asserted. The initial run checked the child but code review found the parent used a workflow label against the status column; this was corrected and the strengthened rerun passes both. Both runs clean their exact owned browser processes/profiles; the final run also preserves the user API instance. Nine focused factory/supplier receipt, supplier RFQ and isolated transport-guard tests, all 22 lifecycle/Portal tests and TypeScript pass. These are disposable technical gates, not live Gmail receipt/index retention, actual process restart or ordinary-user acceptance.

Updated existing `manufacturer-rfq-lifecycle` and `manufacturer-rfq-lifecycle-4`; no new row or original-73 status promotion. AGENTS already permanently requires exact-account/content recovery without resending, so no further rule duplication is needed. Next exact continuation: extend the same evidence contract to uncertain automated follow-ups, then exercise actual isolated API/database restart and incoming-response cancellation. Keep live delivery disabled. Isolated user sequence: reopen an uncertain supplier request → Check delivery outcome → inspect confirmation/deadline → reopen history; an unconfirmed result must not unlock another send.

## 13 September — follow-up Email boundary

The scheduled supplier follow-up now passes its exact request and persisted attempted-at claim as server-only command context. Ordinary Email cannot replace or send its reserved message identity, including body-forged claim fields. Communications checks the current sending claim, outstanding response, due date, parent completion/cancellation and superseding requests before preparation and again immediately before provider IO. A reply arriving during preparation prevents delivery and is classified as confirmed not sent, not uncertain.

Five RFQ tests, the 22-test lifecycle suite and TypeScript pass. An additional focused lifecycle regression proves an acknowledgement arriving during provider preparation prevents the provider call. The first normal scheduled-worker rerun completed the functional path with two no-network provider messages and zero browser/profile leaks, but FAILED its final user-API-instance stability assertion; it is not a full acceptance pass. A stable-baseline rerun is required. No live Email or business-data mutation was performed.

The stable-baseline rerun passes the complete normal composer → genuine-source import/reload → controlled send → reviewed deadline → scheduled worker → reopened outcome gate, with exactly two no-network provider messages. Exit 0 includes zero owned browser processes/profiles and unchanged user API instance. The pre-existing watcher retains parent PID 29604 and API PID 31628; no manual API restart or unrelated process termination was performed. The preceding failed stability gate is retained above rather than erased.

This advances existing `manufacturer-rfq-lifecycle` and `manufacturer-rfq-lifecycle-4`, not a new requirement or user acceptance. Next: evidence-backed supplier/follow-up uncertain receipt recovery, actual test-API restart and complete normal-route incoming cancellation. No original-73 status is promoted. Isolated user checklist: reopen the working Estimate supplier request; confirm prepared versus sent; review the response deadline; inspect the one follow-up outcome; confirm Email cannot replace/resend that tracked follow-up. Live sending remains outside automated acceptance.

## 13 September — retained supplier correspondence consolidation

The older `prepareSupplierRevisionCorrespondence` command previously maintained separate correspondence rather than the consolidated supplier RFQ. It now delegates to the canonical request composer/service and requires an explicitly reviewed supplier; it does not guess one. Preparation remains distinct from sending and from creating the working Estimate revision. Historical messages are not migrated, overwritten or automatically resent.

The reviewed composer exposes Earlier supplier correspondence through exact request/message relationships, with 20-message bounded pages and a total count. Staff can continue an old draft's original text, choose its supplier and review current files. Original attachments remain listed; downloads require retained checksum and storage identity and use the existing byte-verification boundary. Unconfirmed versions remain visible with a review instruction, not an automatic replacement or attachment. Generic Email draft/send routes reject attempts to bypass this review for historical messages.

Technical checks: all 22 Portal/lifecycle tests and TypeScript pass, including compatibility-route preparation/sending through the persistent claim and old-message retention/bypass rejection. The normal-browser `--stop-after-legacy-supplier-correspondence` gate passes 21 disposable historical messages, older/newer pagination, a real retained TXT attachment download with exact bytes, foreign-message rejection, unconfirmed drawing exclusion, explicit supplier review, preparation and reopen. The initial header assertion incorrectly attempted to read a non-exposed cross-origin header; the corrected test checks that header over HTTP without weakening CORS. The successful run independently confirms zero owned browser processes/profiles and preserves the user API instance. No live delivery, provider credentials or business-data copy is used. This is not a live supplier receipt or actual application-restart test.

Existing `manufacturer-rfq-lifecycle` and `manufacturer-rfq-lifecycle-4` and permanent preservation guidance are updated. Original-73 mapping and implementation/user-acceptance statuses are unchanged. Remaining continuation: supplier/follow-up receipt recovery, ordinary Email follow-up protection, actual process restart and incoming-response cancellation; broader lifecycle and user acceptance remain open.

## 13 September — initial supplier delivery claims and partial-result recovery

The consolidated supplier composer now persists its canonical RFQ before provider IO, then claims its exact communication in `supplier_delivery_attempts`. A partial unique index prevents concurrent sending/sent/uncertain attempts for that RFQ; deletion protection retains evidence. Confirmed pre-send failure can retry, unknown delivery stays blocked, and an already confirmed provider message can complete the RFQ projection without sending again. Older failed/sending communications without classifiable attempt evidence remain blocked, not assumed unsent. Ordinary Email cannot send or overwrite the retained supplier message; a database guard also rejects a late concurrent draft save after its delivery claim starts. This is a necessary delivery prerequisite for existing RFQ entries, not new workflow scope.

The composer retains entered values on error, reloads only the request-state projection, distinguishes uncertain delivery from prepared-not-sent and offers **Finish saved result** after provider-confirmed/local-save failure. The normal `--stop-after-supplier-send-partial` gate injects a disposable RFQ-status write failure after provider confirmation, reopens the same request, completes its local result without a second send and continues through the seven-day deadline/scheduled-follow-up path. The initial normal supplier-send/follow-up rerun also passes. The test transport has no external network fallback; optional placeholder Project drawing metadata is not represented as delivered evidence.

Focused service tests cover concurrent calls, canonical persistence before provider access, confirmed partial-save recovery, uncertain blocking across service recreation, explicit pre-send retry, ordinary Email body-token forgery/draft overwrite and immutable claim history. A late draft overwrite is rejected both while sending and after confirmation. TypeScript and the Portal/lifecycle suite pass. Service recreation remains weaker than an actual API/SQLite reopen test; uncertain supplier-send and follow-up receipt reconciliation, ordinary-user acceptance and live-provider operation remain pending. The older standalone supplier-revision-correspondence compatibility path has not been migrated to this consolidated-RFQ attempt contract and must not be counted as covered.

Existing `manufacturer-rfq-lifecycle` / `manufacturer-rfq-lifecycle-4`, permanent AGENTS rules and the isolated clean-workspace inventory are updated. No original-73 status or acceptance promotion. Next exact continuation: consolidate the legacy correspondence delivery boundary, extend exact-provider receipt reconciliation to supplier sends/follow-ups, close follow-up Email-route bypass, and run normal-route restart/incoming-response cancellation. Keep live delivery disabled and preserve all customer evidence.

## 13 September — supplier deadline and scheduled-follow-up safety

Tracing the pending supplier-send gate found two unsafe reset paths: any follow-up error could be retried, and editing the response deadline cleared the attempted-send claim. Both could allow another copy after an unknown provider outcome. Follow-ups now persist sending/sent/not-sent/uncertain state; only explicit pre-send failure can queue a reviewed retry. Deadline changes preserve claims and failure evidence. Claims recheck the due time; a response/cancellation before delivery does not remain falsely labelled sending. Confirmed provider/local-completion failure retains a non-retryable sent outcome. The history explains uncertainty, shows confirmed sent dates and does not offer unsafe retry. The deadline input also had a real normal-browser crash from reading `event.currentTarget` inside a deferred state updater; the input value is now captured immediately.

`--stop-after-supplier-followup` passes the normal application composer → reopen prepared request → confirmed no-network supplier send → exact seven-calendar-day deadline → reviewed deadline adjustment → actual persistent scheduled worker → one follow-up → reopened persisted result. The test uses the generated change-summary PDF; it explicitly deselects preview-only optional drawing placeholders whose missing bytes correctly blocked an earlier run. It does not prove optional-drawing delivery. There are exactly two simulated provider messages, both to the configured factory test address, no live delivery and zero owned browser/profile remnants. The user API instance is preserved during each run. No worker endpoint or browser-local scheduler was introduced.

The 22-test Portal/lifecycle suite passes, plus the focused final follow-up regression, five RFQ/guard tests, TypeScript and 28 Roadmap tests. Service tests cover confirmed pre-send retry, repeat scans, cancellation, service recreation, uncertainty and a deadline edit that cannot clear its claim. Browser process restart/reconciliation and the complete acknowledgement/revised-document cancellation chain remain separate gates; service recreation is not an API restart test. Legacy attempted rows without a classified outcome remain uncertain, not assumed unsent.

Existing `manufacturer-rfq-lifecycle` and `manufacturer-rfq-lifecycle-4` updated, alongside the permanent deadline/claim rule. Original 73-entry mapping and totals are unchanged; no user acceptance is added. User checklist: working Estimate → Request supplier estimate / revision → reopen prepared request → review/send in the isolated test workspace → history → adjust response deadline → reopen and inspect the follow-up outcome. Do not perform live delivery for automated acceptance.

Next exact continuation: harden the initial supplier-request send against concurrent/partial/uncertain delivery and ordinary Email bypass; then extend evidence-based recovery to supplier follow-ups and verify actual API restart/cancellation through the normal route. Existing factory receipt recovery is reusable evidence, not proof these supplier paths already have it. No permission-blocked operations remain in this package; the focused child-process test ran under the existing approved command permission. Preserve unrelated work and live data.

## 13 September — evidence-backed factory delivery recovery

The interrupted continuation now adds **Check delivery outcome** to the retained factory review. Before provider sending, the active attempt retains the connected account, unique RFC Message-ID and a manifest of the saved recipients, subject, HTML and attachment checksums. Recovery makes a bounded SENT search (maximum two matches, ambiguity rejected), verifies exact headers/content and downloaded attachment bytes, then repairs the canonical request/message and records the staff reconciliation event without another send. Missing evidence never unlocks retry. Concurrent late send failures cannot downgrade the confirmed attempt or overwrite its recovered message.

Technical evidence: `--stop-after-source-backed-factory-reconcile` passes the genuine-source normal application chain through factory review, simulated provider acceptance followed by a lost response, the visible uncertain state, Check delivery outcome, confirmed sent state and replay without another copy. Exactly one factory provider message contains the reviewed price-free schedule and retained supplier PDF. This is the production Gmail adapter over explicit no-network disposable transport, not live Gmail. Exact owned Chrome process/profile cleanup is zero; the user's API instance stays unchanged during acceptance. Focused receipt/claim tests cover changed recipients/body/bytes, missing/ambiguous messages, invalid timestamps and concurrent recovery. The 17 related communications/RFQ/transport tests, 28 Roadmap tests and TypeScript pass.

Gmail's official [messages.list documentation](https://developers.google.com/workspace/gmail/api/reference/rest/v1/users.messages/list) documents `rfc822msgid` search. Live Gmail preservation/indexing of these headers is **not verified** here. Older uncertain attempts without receipt markers remain blocked for evidence review; no inference of failure, manual retry bypass or live send was added. Non-Google recovery, browser/API restart recovery, broader supporting-file pagination and the genuine returned-factory-confirmation chain remain incomplete. General supplier/customer delivery is not implicitly migrated to this factory-specific contract.

Existing `procurement-8` and permanent AGENTS delivery-recovery rule updated; all original 73 IDs remain mapped and no entry is newly user-accepted. Totals unchanged: 23 complete, 77 in progress, 140 not started, 3 blocked and 10 legacy. Ordinary-user checklist: reopen the disposable approved Order → review the retained request → check an uncertain delivery → confirm the sent result and unchanged attachment list. Live delivery remains separately restricted.

Exact next bounded continuation: initial supplier-request successful send → persisted seven-calendar-day follow-up → acknowledgement/revised-document cancellation and restart/retry protection through the normal route, using the existing scheduler and disposable no-network delivery. Do not mark overall intake/conflict, procurement or later lifecycle stages complete.

## 13 September — reviewed factory delivery and persistent duplicate protection

Factory preparation can no longer send before a saved preview exists. The reopened review provides an explicit saved-recipient/message/attachment confirmation and a separate controlled Send action. `factory_delivery_attempts` records a claim before provider access; its partial unique index permits only one sending/sent/uncertain attempt per Order, across connections/restarts. Confirmed pre-send failures retain retryable history. Uncertain provider outcomes stay blocked pending evidence review rather than sending another copy. Provider-confirmed sends retain their identity if later local persistence/projection fails. Draft edits/re-preparation are compare-and-set guarded against active delivery. Ordinary Email sending of retained factory messages requires the internal factory-command context, not a request-body flag. No delivery allowlist was broadened in production.

The new `--stop-after-source-backed-factory-send` browser gate uses a separate explicit `factory-send` test transport opt-in, exact isolated-root/development/test-mode guards and only the two configured `example.test` recipients, with no network fallback. It passes the entire genuine-source customer reissue/acceptance/Order/draft/file-review chain, then reviewed factory send and duplicate replay. Outgoing MIME contains exactly two attachments whose hashes match the retained factory PDF and byte-identical supplier source. There is one factory attempt and one simulated factory provider message; the sent request becomes read-only. Both runs cleaned their exact browser/profile and isolated infrastructure, and the user API instance stayed unchanged during each acceptance run. No live communications or business-data changes occurred.

Technical layers remain separate: 30 lifecycle/communication/delivery/guard tests passed; an added provider-confirmed/local-projection-failure regression also passes. Cross-connection and reopened-SQLite tests establish claim persistence, not a normal-browser API restart test. The final compare-and-set/sent-legend tightening is covered by focused lifecycle tests and TypeScript. The new table is in the clean-workspace inventory. Ordinary-user acceptance, live provider operation and evidence-based reconciliation of an uncertain send remain pending; the current UI explains the block but does not yet resolve it. The general customer/supplier delivery flows have not all been migrated to this factory-specific attempt contract.

Existing `procurement-8` and permanent AGENTS delivery rules updated; counts and original 73-ID mapping remain unchanged. Next ready work: evidence-backed uncertain-factory-outcome recovery and the initial supplier-send/seven-day-follow-up normal-route gate, retaining existing restrictions. Do not mistake this factory-send slice for genuine returned-factory-confirmation or overall procurement acceptance.

## Canonical factory supporting-file selection

The saved factory review now offers bounded current canonical Project files from the connected Google account, excluding customer/internal document classifications, Google-native exports and missing content checksums. Staff explicitly review selected files before saving; up to 20 are snapshotted with provider/account/file identity, filename, revision and checksum in immutable `factory_attachment_reviews` rows linked to the exact saved communication. The price-free factory schedule remains included. Prior messages/selections remain unchanged. Changed or foreign-account/Client/Project files are rejected; checksum verification remains at the actual byte-delivery boundary. No files are uploaded or copied by selection. Previously selected files missing from the offered list remain visible for deselection rather than trapping recovery.

Normal-browser evidence: genuine-source customer reissue/acceptance → staff Order → factory draft/PDF review → exact filed `web-26-1133450.pdf` selection → missing-review rejection → explicit review/save. Persisted assertions prove one immutable selection, retained provider checksum/ID and two communication attachments (schedule plus selected document), still draft, with no factory delivery. The original fixture test initially omitted a required OAuth updated-at value; correcting the disposable fixture passes the focused lifecycle gate, including another account, Client and Project rejection and immutable-history enforcement. Existing 20 lifecycle/renderer tests passed before the extra ownership assertions; the focused affected case passes afterwards. Browser cleanup is zero and the user API stayed unchanged during acceptance. Final exact-ID revalidation bypasses the list-page limit; current-list recovery was added afterwards and remains a focused UI change pending user acceptance.

Roadmap `procurement-8` updated, counts/original 73-ID mapping unchanged. The new table is included in clean disposable-workspace inventory. Remaining: searchable/paginated browsing beyond the first 100 offered files, non-Google provider support, and reviewed factory delivery with durable duplicate/uncertain-outcome handling. The user must review file contents: a canonical file classification/checksum is not automatic proof that it is appropriate for a particular supplier. No live delivery or ordinary-user acceptance is claimed. Next ready step is durable reviewed factory send protection, not a new procurement milestone.

Final verification: 21 lifecycle/renderer/attachment tests pass with all added ownership assertions, TypeScript and 28 Roadmap tests pass. A fresh complete normal-browser rerun passes after the exact-ID and missing-list recovery changes; its inventory explicitly reports zero `factory_attachment_reviews`, and cleanup independently reports zero owned browser trees/profiles. `manufacturer-rfq-lifecycle` continuation was reconciled as well. Ordinary-user acceptance remains: reopen the disposable Order, open Supporting Project files, review and select an appropriate document, confirm review, save, reopen and verify filename/revision alongside the price-free schedule. Do not send live factory emails.

## Reviewed attachment bytes — delivery prerequisite

Audit before additional factory attachment selection found that the shared mail service loaded current Drive/managed bytes without checking their retained checksum. It now verifies SHA-256 evidence before creating a provider draft or sending; legacy 32-character Drive MD5 values previously passed in the `sha256` field are verified as MD5 rather than mislabelled or ignored. Invalid/mismatching evidence raises an actionable 409 before draft persistence or Gmail invocation. Newly loaded bytes return their actual size and SHA-256; missing prior checksums are not claimed to establish a prior reviewed version. No issued document, provider file or retained source is rewritten.

Nine focused attachment/communication tests pass inline/base64/managed/Drive branches, changed content at the same provider ID, legacy checksums, invalid evidence, zero send calls and unchanged saved draft on rejection. The fresh-storage genuine-source browser journey passes customer MIME send/failure/retry, exact customer acceptance, factory schedule download/hash, draft validation/save/reopen and zero owned cleanup with the user API instance unchanged. The changed-provider rejection is service-level evidence, not a separate normal-browser external-rename test. No live emails or live business writes occurred.

This is a necessary shared prerequisite to `procurement-8`, not completion of its additional-attachment picker or factory delivery step. AGENTS now permanently records reviewed-byte verification. Roadmap status counts, original 73-ID mapping and user acceptance remain unchanged. Next: use this guard behind explicit canonical Project-file selection, retain the selected provider revision/checksum in the reviewed factory draft, and add reviewed delivery with durable duplicate/uncertain-outcome protection before enabling factory sends in automated acceptance.

## Persisted factory draft review and recovery

The staff Order journey now reopens its saved factory request with recipient, subject, readable multiline message and an exact saved-PDF link resolved through the configured API origin. Saving a reviewed edit creates a new communication/attachment metadata identity while retaining the previous message and the same document bytes. An expected communication ID rejects stale-window writes; unchanged saves reuse the current version. Sent requests remain read-only. The UI prevents duplicate clicks, retains values after validation/save failures, and distinguishes saved-but-refresh-failed from an unsaved failure. Unsafe legacy requests expose re-preparation before editing rather than a misleading price-free download.

Technical evidence: 20 focused Portal/lifecycle/renderer tests pass, including saved/reopened content, safe attachment linkage, retained old attachment metadata, escaped message text and stale-edit rejection. TypeScript and 28 Roadmap tests pass. Both genuine-source browser runs pass PDF download/hash, edited draft save and full application re-entry restoring that text. The final run also passes invalid-recipient rejection without persistence or text loss, correction and double-click save, then reopen. Owned browser/process/profile cleanup is zero; the user API instance remained unchanged during each run. The final metadata-only follow-up clears old provider/thread IDs and updates the Email snippet/date, with focused regression coverage. This is not connected factory sending or supplemental provider-attachment selection. No live email, business mutation or customer acceptance occurred. Browser runs use fresh schema-only storage.

Roadmap updates: `procurement-8` tracks this delivered review surface while keeping reviewed delivery/extra attachments outstanding. `procurement-10` and `end-to-end-commercial-lifecycle` now explicitly distinguish older fixture/API-assisted confirmation evidence from genuine-source normal-route acceptance. No statuses/counts changed and the original 73-ID mapping remains intact. Next ready work: canonical reviewed factory attachment selection/delivery and the original supplier-request send/seven-day follow-up normal-route gate, using explicit no-network provider transport and persistent scheduled-job safeguards.

## Genuine-source customer acceptance and factory document boundary

The resumed journey passes exact successor PDF download in the customer Portal, all five canonical Position decisions, partial-acceptance blocking, double-submit/reload preservation of one canonical pending-staff-approval Order, and normal staff approval. It does not send a factory message or use the retained manufacturer quotation as a returned factory confirmation. Prior complete-journey confirmation claims are now qualified: those downstream checks used disposable fixture/API shortcuts and do not establish genuine returned-source acceptance.

Read-only code tracing found that factory preparation attached `createOrderDocument(...staff-approved)`, whose shared customer renderer includes selling prices, VAT totals and customer terms. Factory preparation now owns a separate `factory-price-free-v1` document snapshot from the exact accepted Position IDs. Its allowlisted projection excludes commercial amounts/terms, its PDF uses a separate price-free schedule, and existing accepted/customer-issued PDFs remain unchanged. An unsafe unsent legacy draft is blocked before delivery; explicit preparation creates a replacement communication while preserving the old evidence. The staff route offers preparation again. Supplemental reviewed attachments and complete factory composer reopening remain outstanding, not silently claimed delivered.

Focused evidence: 20 Portal/lifecycle/renderer tests and TypeScript pass, including old-draft rejection, explicit recovery, safe-document reuse, prior communication retention and unchanged customer PDF identity. The first extended browser run reached factory preparation but failed in its new PDF-copy assertion because the runner omitted an import; cleanup verified zero owned browser processes/profiles. The import is corrected; the final browser/PDF result is recorded below when complete. Live records, live delivery and user acceptance remain untouched. Existing Roadmap entries updated: `canonical-order`, `procurement`, `procurement-8`; original 73-ID mapping and status totals are unchanged.

Final browser result: `--stop-after-source-backed-staff-order` passes the complete genuine-source chain in fresh schema-only storage with no live database copy. The factory PDF contains all five Position references and reviewed RAL 7016, excludes customer totals/validity/terms, and matches saved SHA256 `c2b034b02804859a725de7a0f25c31ee0deba62d2a67f3b5444601e9b60dccae` (3 A4 pages, 163843 bytes). Rendered pages 1 and 3 were independently inspected; source drawings remain intact. Exactly one simulated customer delivery occurred after the tested failure/retry; no factory delivery occurred. Owned browser/process/profile checks are zero and the user API instance stayed unchanged during the run. All 28 Roadmap tests pass. `manufacturer-rfq-lifecycle` continuation and the permanent supplier-facing document rule were also updated. No statuses/user acceptance advanced.

Continuation: integrate the reopened reviewed factory draft/attachments before controlled factory sending; the new preparation-again route is only a bounded recovery affordance, not a complete composer. Initial supplier send/overdue follow-up and complete multi-supplier reissue remain separate ready package gates. Do not expand procurement into unrelated production/dispatch work. Ordinary-user checklist: accept every Position of the disposable released Estimate, confirm one pending-staff-approval Order, approve as staff, prepare (do not send) the factory schedule, and review its Positions/drawings with no customer prices. Keep live records and external delivery out of this checklist.

## Connected genuine-source customer reissue — technical gate passed

`--stop-after-source-backed-customer-reissue` now continues the genuine EKO PDF journey through the normal customer Send action, one visible simulated provider failure, retry, immutable successor issue and full page refresh. It uses the production Gmail MIME builder and delivery/issue commands, not a direct database status update. The isolated transport accepts only the explicitly configured customer `example.test` recipient, refuses every other Gmail write, has no network fallback, and retains MIME locally in the owned temporary workspace. The application still applies its test delivery policy. Default test runs remain delivery-disabled; enabling the new mode also requires development, test-journey mode, the uniquely owned workspace prefix, an explicit `customer-reissue` transport opt-in and exact test addresses. No production policy or live integration was loosened.

Final normal-browser evidence: two send attempts, one successful simulated provider message, exact PDF bytes matching saved document and outgoing attachment, one successor release and Follow Up, parent revision state `revised_customer_estimate_issued`, preserved original issued PDF, and refreshed UI restoring successful issue with another issue disabled and no repeat delivery. Reviewed PDF is six pages; final-run SHA256 `e85041a65ac0c0d22e47af940698f01d7cd64c0023ba614c3c41ee457174d926`. Four transport/guard tests pass default-write refusal, exact To/CC/BCC restriction, failure/retry and pre-IO environment rejection. Both normal reissue runs pass exact browser/profile cleanup and user API instance preservation. This is no-network provider-boundary technical verification, not live email delivery, ordinary-user acceptance, full supplier-request sending/follow-up acceptance, multi-supplier whole-schedule reissue or Portal production readiness.

Existing entries updated: `quotation-12`, `quotation-15`, `manufacturer-rfq-lifecycle` continuation. Counts and the original 73-ID mapping remain unchanged. Exact next ready step: extend this released genuine-source successor through normal customer Position acceptance → one canonical pending-staff-approval Order, retaining the same released PDF/Position identities and customer-safe projection. Reuse the new no-network transport only where delivery is actually necessary; keep factory sending blocked. Multi-supplier schedule review and initial supplier send/overdue-follow-up normal-route coverage remain explicitly separate outstanding package gates.

## Readable reviewed customer Email and document wording

The customer Message editor now projects retained HTML into readable multiline text using an inert, unmounted template. Block breaks/entities are retained and linked addresses remain visible. If the user leaves that text unchanged, sending preserves the exact prepared HTML; edits are escaped and line breaks encoded so typed angle brackets cannot become markup. Failed sends keep entered text/subject, and refresh restores the saved failed draft. The textarea now honours its existing eight rows instead of inheriting single-line height. Customer PDF captions say `Manufacturer drawing` / `Product drawing`; internal `Source-owned` / `Canonical` descriptions were removed without changing source images, issued PDFs or the document layout.

Technical evidence: the disposable quotation component/production-router browser passes readable default text, exact unchanged-HTML round trip, editing with `&` and literal angle brackets, send failure → refresh → successful simulated-provider retry, exact escaped stored body, one issued record/release/event/follow-up and reviewed withdrawal retaining evidence. The final focused run also passes multiline height. The genuine-source normal-route preparation/PDF gate passes with readable Message text; six-page PDF SHA256 `196aca45bc60498ab34df589709bd8fce68d51323a289fd2f77efa0e13712b45` matches canonical document and outgoing attachment. TypeScript, 26 quotation tests and 28 Roadmap tests pass. All owned browser/profile cleanup checks pass; no live delivery or business-data writes occurred. The normal source-backed run precedes the final CSS-only height correction; the focused real-component browser verifies that final correction.

Roadmap: updated `quotation-12` and the `manufacturer-rfq-lifecycle` continuation; statuses and user acceptance remain unchanged. Exact next package step: join the genuine-source preparation route to the production send/reissue boundary using explicitly guarded no-network disposable provider delivery, including failure/retry and immutable successor release checks. Do not relabel the current normal preparation gate or the separate simulated-provider component send as that combined acceptance. The existing delivery-disabled full-journey transport must remain fail-closed unless a separate explicit disposable-only mode is implemented and verified.

## Connected customer preparation and PDF corrections

`--stop-after-source-backed-customer-preparation` now passes the complete normal route through explicit staff approval of the four additional source Positions, customer preview, reviewed terms and saved not-sent Email/PDF preparation. Two real omissions were found and repaired: the EKO WEB adapter retained its explicit Colour heading only in raw evidence, so customer specifications omitted it; and prepared-document hyperlinks used the frontend origin, returning the HTML application instead of the PDF in split-port development. The adapter now retains the unique Position-local Colour heading, original text, attachment/page/block identity and bounding region in the source-specification contract. The customer projection labels this unsided evidence `Colour`, never inventing distinct inside/outside finishes. Missing or ambiguous headings cannot borrow a vent's colour or another Position's evidence. Prepared/get/send/withdraw response links resolve through the configured API origin. Existing issued documents and live imported snapshots are not rewritten.

Evidence: genuine EKO source/ambiguity test passes; 26 quotation projection tests, TypeScript and 28 Roadmap tests pass. The final fresh-storage normal-browser run verifies five customer Positions, retained review snapshot, exact recipient, only the original issued quotation, and prepared PDF download SHA256 `7f10bbec2693aec0b480959405d3617d414a8ca26220e26c577c0749a81d511e` matching both canonical PDF and Email attachment. The six-page PDF contains the reviewed Colour and terms; the rendered schedule was independently inspected. Disposable customer total is £8,384.68 from saved costing, not an approved commercial policy or live customer offer. Earlier script selector/DOM-result mistakes were corrected; each run cleaned its exact browser/profile and isolated resources, and the user's API instance remained unchanged during acceptance.

Updated existing entries: `quotation-10`, `quotation-12`, `manufacturer-rfq-lifecycle` continuation. No acceptance/status totals advanced. Exact next step: the preparation screenshot exposes raw HTML in the Message textarea; replace that normal-user editing experience with safe plain-language editing while preserving prepared content and failure recovery, then exercise controlled send/reissue. Customer PDF also still uses the internal-sounding caption `Source-owned manufacturer drawing`; include its wording in the bounded customer-output review, not a product-page redesign. Prepared is not sent, and neither this test nor service tests establish user acceptance, multi-supplier full reissue, public Portal readiness or clean provisioning.

## Source-backed overall review — normal-route evidence

The new `--stop-after-source-backed-overall-review` gate passes against fresh schema-only disposable storage. It extracts Window 001's actual reference, quantity and dimensions from the retained EKO PDF for a test-owned issued opening, and verifies page 1 explicitly contains `RAL: 7016 (Anthracite grey) Matt`. The disposable customer requests that finish and confirmation of WEB/26/1133450. Normal routes perform customer submission → working revision → supplier composer → genuine upload/analyse/review/final import/reload → repeat import → exact disposable provider email → reviewed filing → supplier Position review → explicit Link returned revision using that same canonical document → overall verification. Persisted assertions prove the imported source retains the issued Position ID, there are five rather than six working Positions, two implemented requested checks, an overall verified timestamp, unchanged original issued Position identity and only one issued customer quotation. No API fixture sets verification. The first run exposed a missing test step (explicit overall source linking); the corrected run passes. Both runs cleaned their exact browser/profile and isolated API/database resources; the user-owned port 3001 instance remained unchanged during each run.

This is one genuine-source requested-Position verification gate, not full customer reissue or multi-supplier schedule acceptance. Four additional source Positions remain part of the working schedule; the next customer-document journey must explicitly review the complete resulting schedule and commercial output before preparation/reissue. The two-supplier reference-only gate remains separate evidence. Do not infer that this new single-source test establishes all original W01/D01 fixture relationships, all supplier differences or customer acceptance. Roadmap `manufacturer-rfq-lifecycle` retains in-progress status and no totals change.

## Latest bounded continuation — supplier request result consistency

Overall-review gate audit: unlike independent supplier reviews, `verifySupplierRevision` accepted an approved-difference flag with a reason even when the returned value or its source reference was absent, and did not validate Position ownership. The entire submitted batch now validates Position membership against the exact issued customer review, nonempty/unique Position-field identities, and complete original/expected/returned values plus both source references and reason for an approved difference before changing saved checks, approval or history. Ordinary incomplete checks can still be saved unresolved. Focused disposable regression evidence rejects each missing approval input, a foreign Position and a duplicate requested/unrelated field, and proves prior checks, verified timestamp and immutable history remain unchanged. This is a service-level safeguard repair, not normal-browser proof of a completed overall review/reissue journey; the truthful source-backed baseline continuation below still applies.

New supplier-request responses previously assembled a smaller object than reopen/retry responses, omitting persisted creation/update dates and response/follow-up fields. Immediate history could therefore display `Invalid Date` until a context refresh. Creation now reads the saved record through the same canonical projection. Three focused RFQ tests pass, including comparison of every reopened field against creation and replay, ownership/delivery rejection and partial-save recovery. The normal application `--stop-after-supplier-draft` gate passes customer changes → working revision → prepare → continue editing with the exact persisted date visible and one unsent request. An initial browser assertion targeted the reply chooser instead of request-history articles; that test selector was corrected. Both runs independently verified zero owned browser processes and profiles. All 28 Roadmap tests pass. No live email or business mutation occurred.

Roadmap entry updated: `manufacturer-rfq-lifecycle`; implementation/technical evidence only, no status/count or user-acceptance advancement. Port 3001 remained user-owned: the existing source watcher replaced initial PID 14492 after the server edit with PID 42848, parent 29604, command `node.exe --watch-preserve-output C:\Github\QuoteSync\web\server\index.js`, runtime v15. Codex did not stop or replace that listener; the isolated browser run confirmed its API instance stayed unchanged during acceptance and cleaned its own infrastructure.

Exact continuation: establish a truthful disposable issued schedule and requested Position changes against the retained source before walking overall verification and customer reissue. Inspection confirms canonical import mapping uses reviewed Position applications/existing source links, then a unique reference/quantity/dimension match within the same quotation aggregate; the current synthetic W01/D01 baseline does not meet that evidence. Do not claim that two resolved quotation-reference checks prove the requested black finish or that a fixture/API reissue shortcut is normal-route acceptance.

Baseline: the original 73 entries reconciled as `in_progress` on 11 September 2026. The executable mapping is `src/features/developmentRoadmap/roadmap.workPackages.ts`; every baseline ID still appears exactly once. The live Roadmap now shows 77 in progress because Service, the existing qualification child and the newly separated Basic RAMS requirement have implementation evidence; the new Commercial RAMS row remains not started. Those later rows do not alter or duplicate the original 73-entry mapping.

Grouping does not change delivery status. An entry remains in progress until its own remaining implementation and technical verification are complete, and its user-acceptance statement is updated independently. A merged or superseded row is not counted as delivered functionality.

## Dependency order

1. **CRM workday and Enquiry intake (5)** — canonical dashboard projection, exact-record navigation, ownership/next-action continuity and Enquiry intake. No external dependency for the internal foundation. Ordinary-user acceptance remains required.
2. **Email, Files and supplier-document intake (23)** — builds on CRM context. Individual-message reading, exact selected-attachment ownership, evidence-backed link-or-create intake, stable partial-failure retry, supplier-folder conflict classification, save-as-new-revision, progress/failure/retry feedback and import handoff have disposable normal-route evidence. A working Estimate opens guided supplier RFQ preparation; the exact selected inbound response/document can link that RFQ, record Quote Returned once and open Manufacturer Import review for the same working Estimate. Remaining work is ordinary-user acceptance plus wider provider operations. Genuine provider acceptance requires configured Gmail/Drive, but no live filing is authorised for EF-CL-028.
3. **Customer Estimate, quotation and release (23)** — reuses manufacturer evidence and the existing document renderer. Reviewed Estimate-owned validity/terms/exclusions, download, deterministic preparation, failure/retry, provider-confirmed issue, immutable release, three-day Follow Up and append-only issued/superseded/withdrawn states pass one disposable normal-route journey. Remaining implementation is generic configured drawing coverage shared with Package 7; corrected EF-EST-2026-055 output and the full issue/lifecycle journey still need user acceptance. Production sending remains a controlled external dependency.
4. **Portal, canonical Order and procurement (7)** — the bounded development journey now passes released Estimate → customer changes → working successor → returned-revision checks → reissue → exact Position acceptance → one canonical staff-gated Order → factory-confirmation comparison → customer final sign-off → reviewed signed-PDF return. Delivery is preview-only. Ordinary-user acceptance plus production identity, tenant/staff authority, e-signature/legal policy and controlled provider delivery remain external gates; deposit, production, dispatch, delivery/installation and warranty are still partial.
5. **Quote comparison and reusable technical documents (4)** — canonical Manufacturer/Supplier and Product/System selection plus exact Project/Position applicability review now have disposable normal-route proof. Explicit unmapped comparison evidence remains unresolved after reload. The implementation slice is technically verified; exact Ty Clai presentation acceptance, older commercial-breakdown recovery and production customer-safe projection remain the joined gates.
6. **Installation operations (3 original entries, plus the later bounded Service/qualification/RAMS scope)** — depends on an Estimate-owned costing snapshot. Existing workforce/materials work is reused. Qualification records and Basic RAMS now have service/UI foundations and focused persistence evidence; provider-backed Service files, installer-pack integration, normal-browser acceptance, scheduling and unsupported catalogue evidence remain explicit dependencies. Commercial RAMS is recorded separately as not started.
7. **Configurator and drawing proof (3)** — can progress after customer-output contracts are stable. Existing B92 proof is reused; broader product families remain evidence-dependent.
8. **Platform boundary, clean provisioning and bounded cleanup (5)** — extracts stable shared boundaries after the first journeys. Clean database/storage support exists; tenant-neutral assets/package manifest remain implementation gaps. Cross-tenant Core ownership and production RBAC are genuine release blockers recorded outside this 73-entry cohort.

## Package gates

Each package is completed as one user journey and shared-service pass: remaining implementation → focused service/API tests → normal-route browser verification including failure/recovery → Roadmap evidence update → coherent checkpoint/push → short user-acceptance checklist. Prior evidence is reused unless the package changes its contract.

The first implementation pass starts with: **start the day → see real overdue/today work → search/open the exact Client, Enquiry, Project, Estimate, Order or follow-up → complete work and set the next action**. It replaces browser-local dashboard inference rather than adding another CRM store.

## Current continuation point

### 13 September: exact field-source ownership prerequisite (Packages 2/4)

Overall customer-change checks previously shared the parent's one returned-document identity, even when two suppliers had returned independent files. Each field can now retain its explicitly reviewed canonical document, displayed filename and version/checksum. Only documents returned against active requests for this exact revision are offered/accepted. Missing multi-source selections, unavailable sources and silently omitted previous selections fail before saved checks/history are changed. Changed document evidence makes its own checks stale; preparation freezes the selected identities and earlier snapshots remain retained. Legacy single-source preparation shapes remain compatible.

Technical evidence: 22 Portal/lifecycle tests pass, including rejected source ownership without mutation and exactly one stale field when the second document changes; TypeScript and 28 Roadmap tests pass. The normal application `--stop-after-multi-source-field-review` gate passes with the genuine EKO PDF and Zyle DOCX through separate exact-message filing, supplier review, missing-source error, explicit per-field sources, persisted unresolved checks and reload/reopen. It intentionally does not assert that the synthetic issued Positions correspond to those source Positions, and creates no customer reissue. No-network disposable provider transport only; no live OAuth, filing or delivery. Successful run `0b02dd69-2542-4080-bf6d-34eba0112a90` independently verifies zero owned browser/process profiles and unchanged user API instance `8a885d8f-bc8c-4ffe-aede-c632ced6c7a7` (watched child PID 39392, existing parent 29604). Earlier runs exposed a scripted batched-input issue and a watched-API baseline change; they are not reported as passed acceptance. Sequential field entry and an enabled-action assertion repair the test, without weakening cleanup/identity gates.

Roadmap updated: existing `manufacturer-rfq-lifecycle`; permanent source-ownership rule added to AGENTS.md. Original 73 mapping and statuses are unchanged. Ordinary-user acceptance remains open. Continue with demonstrably matched multi-supplier Position evidence and the joined overall review/customer reissue gate; selecting a source or reviewing its quotation reference is not proof of finish compliance. Suggested user check: reopen a disposable change request with two returned supplier files, choose each field's source, verify missing choices retain inputs, save an unresolved comparison, reload and confirm the same filenames and blocked reissue.

- Package 1 is technically verified and awaits its bounded user checklist plus the authenticated multi-user/history dependencies recorded in the Roadmap.
- Package 2 Email intake/filing and supplier-RFQ preparation/return are technically verified through likely-record link-or-create → working Estimate RFQ draft → exact selected response → destination/revision review → Quote Returned → Manufacturer Import review handoff. User acceptance and external provider operations remain; do not expand into non-Gmail channels or live EF-CL-028 writes.
- Package 3 now has a verified terms review → Download → prepare → failure/retry → immutable issue/release → Follow Up → reviewed withdrawal browser journey, plus service/Portal proof that a newer revision supersedes earlier offers without changing evidence. Continue at the generic B92 document drawing item shared with Package 7, then run the consolidated ordinary-user checklist and separately authorised controlled delivery.
- Package 4 now has one normal-route disposable browser proof from customer change request through canonical Order and reviewed final confirmation. The Portal/Order/supplier-neutral confirmation slice is implementation-ready and awaits its short user checklist; public access, live delivery, e-signature/legal policy and later procurement/delivery milestones remain incomplete. Continue with Package 5 comparison/technical documents while preserving the shared Package 3/7 configured-drawing dependency.
- Package 5 now has service/API proof and a normal-route disposable browser journey for canonical retained-document registration, exact Project/Position applicability, explicit customer-visibility approval, persisted reload and no provider-binary copy. Comparison hydration preserves reviewed `unmapped` evidence. All four entries remain in progress pending the short ordinary-user checklist; the Ty Clai visual/PDF review and older commercial-breakdown recovery remain explicit. Continue with Package 6 installation operations without treating technical proof as user acceptance.
- Package 6 has focused persistence proof for team/Service routing, internal service targets, installer qualification validity/evidence, and immutable Basic RAMS revisions. Its current continuation point is normal-browser installer qualification and Basic RAMS acceptance, followed by canonical provider-backed Service attachments and installer-pack integration. The consolidated supplier estimate/revision composer is part of Package 2: its bounded browser preparation/failure/retry check passes, but the complete incoming-response and reissue browser journey, uncertain-response linking UI and controlled provider delivery remain outstanding.
- Package 2 response-state correction: acknowledgements now persist as review-required and emit an acknowledgement event, never Quote Returned; later acknowledgements preserve received-document state. Replays return the same next action and refuse a conflicting supplier/Estimate relationship. Saved supplier requests now expose a bounded mailbox reply selector and reuse the existing filing picker. The disposable browser run uses production components and HTTP routes with a fixture Gmail adapter: two same-thread messages on separate bounded pages open their own bodies, and the selected acknowledgement persists without Quote Returned. Browser/profile cleanup independently reports zero remaining; the existing API baseline is preserved. Continue at the new selector's document-filing/import handoff through customer reissue, including failed send/retry and multi-supplier resolution. The complete normal-application journey and ordinary-user acceptance remain outstanding.
- Package 2 filing continuation: the browser now proceeds through reviewed filing, an injected post-storage linking failure, clear partial-success feedback, retry with exactly one stored file, persisted revised-document receipt and the exact saved-document import event. The provider adapter writes and verifies disposable bytes; it does not prove real Drive or Manufacturer Import processing. Communications and lifecycle paths now share receipt-state updates so filing cancels pending follow-up and retry repairs incomplete receipt projection. Seven communications relationship tests and the 22-test Portal/procurement suite pass. Next is the full application's extraction/review/reissue journey and multi-supplier aggregate completion, with ordinary-user acceptance still separate.
- Package 2/4 multi-supplier receipt gate: verification and customer preparation now check every non-cancelled, non-superseded supplier request. An acknowledgement is still outstanding; errors name the suppliers requiring documents. Sending a prepared customer email rechecks this state before provider delivery while preserving recovery of already-sent evidence. The disposable Portal/procurement test proves one returned document plus a second acknowledgement blocks preparation, and new supplier work after preparation blocks send without changing the prepared record. Staff verification feedback reports remaining work. This is a receipt gate only: supplier-specific field-check provenance, complete application browser reissue and ordinary-user acceptance remain outstanding.
- Package 2/4 source-bound review: field checks now retain returned source kind, document identity, revision and available checksum as a source identity. Relinking changed evidence clears verification; partial rechecking cannot reuse checks against the earlier source. Previous checks are snapshotted in immutable review history, general checks update without accumulating NULL-position duplicates, and interrupted review saves remain unverified. Disposable lifecycle proof covers replacement, partial/full recheck, history immutability and reissue rejection. This still uses the parent's selected returned document: independent per-supplier field-review sets, a history UI and the complete browser reissue journey remain the next work, not delivered scope.
- Package 2/4 review reopening: the staff form previously ignored persisted checks and the change-detail projection omitted imported supplier-document choices. It now reuses the canonical revision detail, restores saved field values and linked source metadata, isolates forms when switching requests, and retains inputs on failure. Staff-only View details exposes immutable previous checks through ten-snapshot history pages with retry and paging. The disposable production-component browser/HTTP run verifies twelve persisted snapshots, reopening, failed-save preservation and request switching; exact owned browser/profile cleanup is zero and the user API baseline is preserved. The lifecycle suite passes 22 tests, the final aggregate-reopen check passes separately, and TypeScript passes. These are bounded technical checks, not full normal-application reissue or user acceptance. Continue with independent per-supplier field-review ownership, then the complete extraction/import/reissue journey; original cohort/status totals are unchanged.
- Package 2/3/4 preparation binding prerequisite: tracing multi-supplier review exposed that a fresh verification could unlock a customer Email prepared against earlier supplier evidence. Customer preparation now snapshots the exact reviewed source, field checks and active supplier-response/document identities, includes that snapshot in its retry identity, and compares it again before provider sending. Changed evidence requires a new reviewed preparation; the previous PDF/draft remains retained, and already-issued/provider-sent recovery stays intact. The disposable source-change/re-review test proves the old preparation cannot send, a new preparation has a distinct identity, and retries reuse it. Eight customer quotation operational tests pass; the existing disposable browser terms/download/prepare/failure/retry/issue/withdraw journey passes without live delivery. This is a required reissue safety boundary, not delivery of independent per-supplier field review or full source-import/reissue browser acceptance. Continue at that unchanged multi-supplier gap; no Roadmap item is newly marked complete.
- Package 2/4 independent supplier field reviews: each active supplier request now owns immutable review records over its exact retained response-document set, selected source, Position/field checks and staff identity. A document belonging only to another supplier is rejected. Each supplier's latest source-backed review is checked independently before overall verification, preparation and send; changed response evidence reopens only that supplier review. Retried saves reuse their record and repair partial overall-status invalidation; later supplier reviews cannot reuse earlier overall approval. Staff controls select the supplier and its retained document, preserve entered fields on failure, and show the next overall-review action. Existing overall customer checks remain a separate final coverage check; automated supplier field extraction/mapping is not claimed. Technical evidence: two-supplier service checks cover separate ownership, unresolved fields, changed evidence, immutable records, idempotency and partial recovery; the production-component/HTTP disposable browser saves fields with double-submit protection alongside existing filing/reopen/history checks and zero owned browser/profile remnants. 22 lifecycle tests and eight quotation operational tests passed before final focused recovery/freshness refinements; the final focused customer-change test and TypeScript pass. Next run the full application two-supplier import/review/reissue journey and expose older supplier-specific review versions for staff inspection (already retained immutably). Ordinary-user acceptance remains open; no statuses/counts advanced.

### Supplier-revision acceptance sequence (not yet user accepted)

Supplier-specific history continuation: staff can now expand View details → supplier review versions for the selected request, showing retained field values, before/returned source references, reviewer/date and captured source revision. The endpoint enforces the exact parent/request relationship and uses ten-record pages; history includes unresolved earlier decisions rather than replacing them. Service checks prove separate supplier histories and eleven-record pagination; the production-component/HTTP browser opens the saved supplier version and its source references, with zero owned browser/profile remnants. TypeScript and all 28 Roadmap checks pass. No ordinary-user acceptance is implied.

Full-journey runner audit: `scripts/run-complete-customer-order-journey-browser.mjs` still clicks the removed `Prepare supplier change preview` control and creates placeholder returned supplier-document metadata rather than importing a genuine source. Its historical Portal/Order evidence must not be treated as current acceptance of the consolidated supplier composer, independent supplier reviews or Manufacturer Import. Next update the normal-application journey to the current controls and use an exact retained source or byte-identical disposable copy through analyse/review/final import/reload, while retaining the current delivery and process-ownership restrictions. Do not rerun the stale script and describe a fixture/API shortcut as the requested full-source acceptance.

Full-journey isolation repair: before updating/running that journey, its raw copy of the live `quotesync.db` has been removed. The runner now uses `initializeIsolatedJourneyDatabase` to initialise the real schema in a new disposable path, refuse existing database targets and verify zero business records, OAuth/provider mapping and Portal evidence before adding its explicit fixtures. Schema initialization and seed setup now sit within the runner's cleanup boundary. The focused test verifies the empty inventory, integrity and refusal to reuse an existing target; it starts no API and does not copy live attachments or connection state. This is a safe acceptance prerequisite, not full journey acceptance or approval of customer release provisioning: tenant-private defaults/build assets remain a release blocker. Next replace the stale composer navigation and placeholder supplier source, then execute the requested full-source two-supplier journey.

Normal-route supplier-draft acceptance: the runner now follows the current consolidated composer and offers an explicit `--stop-after-supplier-draft` gate. It checks occupied test ports before starting, verifies the isolated API runtime contract, preserves the user's API instance during the run and cleans owned browser/Vite/API processes on completion/error/signals. This actual application run exposed a defect hidden by component fixtures: Portal creates the working successor server-side, but App's loaded Client did not contain it, so Open working Estimate rendered no workspace. The handoff now loads that Client's Estimate data, adds the exact missing successor without replacing unrelated loaded edits, then navigates; progress/failure keep the revision available for retry. The normal application run passes customer Portal changes → staff working revision → consolidated supplier draft/reopen with one unsent request. Browser cleanup proves zero owned processes/profiles; independent port inspection confirms test ports 3104/5276/9416 are absent and only the user-owned API remains on 3001. This gate uses disposable context/document metadata and does NOT prove source import or customer reissue. Next replace placeholder supplier evidence with genuine retained bytes and continue the two-supplier analyse/review/final-import/reload/reissue gate.

Normal-route genuine-source import gate: `--stop-after-manufacturer-import` now continues the actual application from the reviewed EKO supplier draft into file-picker upload → identity confirmation → extraction review → explicit final import → browser reload. The source is a byte-identical disposable copy of `docs/Supplier_Quotes/John_Wingfield/web-26-1133450.pdf`, SHA-256 `d1f34d3fd36ef40e4fb1b3ccbddc96b96837fdfd86f598af9c2b189f674f1899`. The run verifies zero canonical supplier/Products rows before approval; five distinct persisted source-owned costing rows totalling EUR 7,885.45 afterwards; EKO commercial identity and available source-visual provenance; confirmed persisted import-operation status; original issued Estimate unchanged; and exact retained source bytes. A guarded test-only rate preload supplies a labelled 0.84 market rate without a remote FX dependency; no parser, extraction or import response is mocked. The normal-browser run passes with zero owned browser/profile remnants and unchanged user API instance. This is one-source import evidence, NOT completed Manufacturer Import acceptance in all dimensions: retry/rollback depth remains covered separately and must be joined where affected. The supplier reply/file-to-request relationship, second supplier, complete review/reissue and ordinary-user acceptance remain outstanding. No live provider connection or delivery was used.

Filed-source reuse repair: tracing the join from provider-backed Files to Manufacturer Import found an early return by canonical document ID before checking current bytes or supplier. That could reopen old evidence after an external file replacement. The service now validates content availability and commercial supplier first, compares the current provider bytes with the retained hash, reuses only unchanged evidence and returns an actionable 409 for changed content or conflicting supplier. The earlier binary and commercial evidence remain unchanged; changed content requires reviewed separate-document revision filing rather than silent overwrite or reuse. Seven focused multiformat tests pass. A separate genuine-PDF test uses the actual schema/parser and a disposable provider adapter through the real communications import-review HTTP route: changed provider bytes produce the preserved-source/revision instruction, unchanged retry returns the same attachment, no duplicate quote is created and canonical positions/Project Costing remain untouched. This is service/API proof, not a completed browser conflict-recovery journey. Continue the exact reply/file linkage → second supplier → review/reissue acceptance join; no live EF-CL-028 action was performed.

Saved-source recovery feedback: the import component no longer appends an unchanged-provider-file/safe-retry claim to a detected content or supplier conflict. These conflicts offer Open Files with the exact Estimate/document context; the application handler closes the import layer before opening Estimate Files, avoiding competing modal layers. Transient failures retain the selected document and offer retry. The production-component browser test uses injected HTTP failures to verify both conflict types, exact Files event identity and same-source retry; it does not claim actual provider validation or the complete application Files transition. TypeScript passes and browser cleanup independently confirms zero owned processes/profiles with the existing API instance preserved. The existing `manufacturer-rfq-lifecycle` entry is updated; statuses and original 73-entry mapping remain unchanged. Continue the full normal-route reply/file/import/multi-supplier/reissue join, including the Files recovery transition, before declaring that journey technically complete or user accepted.

Normal-route repeat-import audit (12 September): the strengthened genuine-source runner waits for the import UI handoff and a new browser document before checking the reopened Estimate, rather than accepting old DOM text during reload. It exposed a client-hydration race: a completing client load cleared selected identities after route restoration. Selection resets now occur before hydration; the exact working Estimate reopens in the subsequent run. TypeScript passes. The new repeat upload/import gate then FAILS: `uploadManufacturerQuoteForAnalysis` creates a random AUTO supplier quote and revision for every call, and the same genuine PDF is retained under two attachment/storage identities. This contradicts upload-once/retry preservation and supersedes any broad inference of repeat-upload safety from the earlier one-upload browser pass. The regression assertion remains in the runner. All failed runs cleaned exact browser processes/profiles and disposable services/storage; the user API remained unchanged. Next implement a server-authoritative source staging/reuse boundary with Estimate ownership, byte identity, deliberate supplier-conflict handling, concurrent/retry protection and partial-failure recovery; rerun the genuine-source repeat gate before continuing the supplier reply/multi-supplier/reissue join. No user acceptance or Roadmap completion is claimed.

Repeat-upload repair: Upload & Analyse now calls a server staging endpoint rather than creating random client-side quote/revision records. Within the exact Estimate, a unique matching SHA-256 source is integrity-checked and reused; multiple retained matches fail closed for review. Changed bytes create separate evidence, never overwrite the prior source. A shared-connection staging queue and SQLite immediate transaction protect concurrent staging; failed inserts roll back metadata and remove only the newly created binary. Analysis happens after staging, so failed analysis can reuse the retained source on retry. The API helper retains attachment metadata used by Compare Quotes. Genuine-PDF focused tests pass concurrent calls, reopened-connection reuse, rename, other-Estimate isolation, changed content, insertion failure/rollback and ambiguous prior duplicates. The normal application repeat gate passes upload → identity → extraction → final import → real reload → repeat the same source → final import → reload, preserving attachment, five canonical Position and five costing-row identities and EUR 7,885.45. This supersedes the preceding failing-repeat finding; it does not close the multi-supplier response/reissue or user-acceptance gates. No old duplicates or live evidence were removed.

Provider-transport acceptance prerequisite: `tests/fixtures/disposableGoogleTransport.mjs` now supplies a test-only, no-network Gmail/Drive transport for the production provider adapters. Its focused test proves bounded same-thread message pages, exact message/attachment identity, genuine PDF multipart upload/download bytes, canonical folder-property lookup and refusal of mail delivery or unknown external operations. This is not a replacement filing service or parser. It has not yet been wired into the complete application runner, and its in-memory provider state does not establish provider restart persistence. Next wire an explicitly guarded disposable runner preload and dummy encrypted provider configuration, activate it only for the reply stage, and walk the existing supplier request → exact reply → assignment → real commercial Drive service → canonical document → Manufacturer Import path. Keep source processing real, test delivery disabled and live OAuth/provider records absent. No normal-route filing acceptance is claimed from this transport-only test.

Normal-route provider filing join: `--stop-after-supplier-filing` now runs the actual application, genuine-PDF import/repeat/reload gate, then activates dummy encrypted Google configuration in the disposable database, synchronizes the reply through the production Gmail adapter, opens the saved supplier request, selects its exact message and attachment, files through the real commercial Drive service, and opens Manufacturer Import review for the resulting canonical document. Only provider transport is substituted; it has no external network fallback and refuses mail delivery. The preloader rejects production, enabled delivery and non-isolated paths before IO. This route exposed and repaired a production handoff bug: ScenarioCostingWorksheet's duplicate import-event listener called the parent without a document ID, clearing the exact canonical selection. Removing that listener leaves EstimateCommercialWorkspace as the sole event owner. The normal browser run passes, retains one original supplier attachment linked to the saved canonical document, and verifies the exact supplier request/message, filename and Suppliers/EKO destination. TypeScript and preload/provider tests pass. Owned browser/profile cleanup is zero and the user API instance remains unchanged. No live filing or sending occurred. The provider run uses random canonical record IDs, TEST names and EF-format references in the isolated database because the current folder builders reject TEST-format references; that limitation remains a clean-provisioning gap, not a relaxed guard or proof of other-business provisioning. Continue with source-bound field review, second supplier and customer reissue; this gate ends at import review and does not claim requested changes were implemented or user accepted.

Normal-route source review and outstanding second supplier: `--stop-after-supplier-review` continues from genuine provider filing to the staff customer-change view, selects the exact supplier response and records a deliberately unresolved quotation-reference comparison with the genuine PDF reference and page evidence. Persisted review ownership/unresolved count are checked, overall verification remains unset and no customer reissue is created. `--stop-after-multi-supplier-review` additionally prepares a second independent supplier request, verifies its missing-document warning and switches back to the first supplier's persisted fields. This exposed a real composer defect: Continue editing kept the old idempotency key when the supplier/content changed. The composer now fingerprints reviewed content, retaining its key for unchanged retries and allocating a fresh key for changes; server conflict validation is untouched. The staff working-revision card uses the existing canonical Estimate reference instead of an internal UUID. Both normal gates pass, alongside the existing component/HTTP failure, partial-filing retry and duplicate-submit checks, TypeScript and zero owned browser/profile remnants. This proves unresolved and outstanding work remains open, not successful completion of customer-requested changes. Next complete second-supplier response review, approved overall verification and reviewed customer reissue; the newly saved request's missing immediate createdAt display also needs a bounded response-shape correction. No live delivery or user acceptance is claimed; no original cohort entries are newly complete.

Two genuine supplier responses: `--stop-after-received-supplier-reviews` now adds a byte-identical retained Zyle DOCX (`343117-3_EF-EST-2026-004 - Luke.docx`, whose source heading explicitly states price offer 343117-3) to the EKO PDF provider journey. Both independent requests are prepared through the composer, each exact message/document is filed through the commercial provider path, and staff reopens the customer-change review after navigation. The first unresolved supplier snapshot is preserved; explicit staff acceptance of each evidenced quotation reference creates two subsequent resolved supplier snapshots tied to distinct canonical documents. The normal browser gate passes with three immutable review records, two returned document links, no overall verified timestamp and only the original issued quotation. Transport/guard tests and exact browser cleanup pass. This is supplier-reference review, not proof that the customer's requested black finish was implemented: the original synthetic W01/D01 schedule and two real-source schedules are not a proven Position mapping. Before claiming approved overall reissue, align the disposable issued baseline and requested changes with demonstrable source Position evidence, then walk the established overall verification/customer-document route; do not invent source compliance or relabel this gate as customer reissue. Live records/delivery remain untouched; user acceptance stays open.

1. Open a disposable customer's requested changes and working Estimate. Prepare/reopen the supplier requests; confirm prepared is not sent.
2. Review each exact supplier reply, file the selected revised document, and open Manufacturer Import for review without automatic costing approval.
3. In customer-change review, select each supplier response and save its source-backed fields. Confirm one completed supplier does not clear the other.
4. Complete overall customer-change verification, review the customer preview and prepare the revised customer Email. Confirm later supplier changes block that earlier preparation.
5. In controlled test delivery only, complete issue/retry and check the linked revision/history. Live sending and ordinary-user acceptance remain separate authorisations.
