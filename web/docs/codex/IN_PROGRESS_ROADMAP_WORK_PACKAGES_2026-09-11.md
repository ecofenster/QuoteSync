# In-progress Roadmap completion programme

Baseline: the original 73 entries reconciled as `in_progress` on 11 September 2026. The executable mapping is `src/features/developmentRoadmap/roadmap.workPackages.ts`; every baseline ID still appears exactly once. The live Roadmap now shows 77 in progress because Service, the existing qualification child and the newly separated Basic RAMS requirement have implementation evidence; the new Commercial RAMS row remains not started. Those later rows do not alter or duplicate the original 73-entry mapping.

Grouping does not change delivery status. An entry remains in progress until its own remaining implementation and technical verification are complete, and its user-acceptance statement is updated independently. A merged or superseded row is not counted as delivered functionality.

## Dependency order

1. **CRM workday and Enquiry intake (5)** — canonical dashboard projection, exact-record navigation, ownership/next-action continuity and Enquiry intake. No external dependency for the internal foundation. Ordinary-user acceptance remains required.
2. **Email, Files and supplier-document intake (23)** — builds on CRM context. Individual-message reading, exact selected-attachment ownership, evidence-backed link-or-create intake, stable partial-failure retry, supplier-folder conflict classification, save-as-new-revision, progress/failure/retry feedback and import handoff have disposable normal-route evidence. A working Estimate opens guided supplier RFQ preparation; the exact selected inbound response/document can link that RFQ, record Quote Returned once and open Manufacturer Import review for the same working Estimate. Remaining work is ordinary-user acceptance plus wider provider operations. Genuine provider acceptance requires configured Gmail/Drive, but no live filing is authorised for EF-CL-028.
3. **Customer Estimate, quotation and release (23)** — reuses manufacturer evidence and the existing document renderer. Reviewed Estimate-owned validity/terms/exclusions, download, deterministic preparation, failure/retry, provider-confirmed issue, immutable release, three-day Follow Up and append-only issued/superseded/withdrawn states pass one disposable normal-route journey. Remaining implementation is generic configured drawing coverage shared with Package 7; corrected EF-EST-2026-055 output and the full issue/lifecycle journey still need user acceptance. Production sending remains a controlled external dependency.
4. **Portal, canonical Order and procurement (7)** — the bounded development journey now passes released Estimate → customer changes → working successor → returned-revision checks → reissue → exact Position acceptance → one canonical staff-gated Order → factory-confirmation comparison → customer final sign-off → reviewed signed-PDF return. Delivery is preview-only. Ordinary-user acceptance plus production identity, tenant/staff authority, e-signature/legal policy and controlled provider delivery remain external gates; deposit, production, dispatch, delivery/installation and warranty are still partial.
5. **Quote comparison and reusable technical documents (4)** — canonical Manufacturer/Supplier and Product/System selection plus exact Project/Position applicability review now have disposable normal-route proof. Explicit unmapped comparison evidence remains unresolved after reload. The implementation slice is technically verified; exact Ty Clai presentation acceptance, older commercial-breakdown recovery and production customer-safe projection remain the joined gates.
6. **Installation operations (3 original entries, plus the later bounded Service/qualification/RAMS scope)** — depends on an Estimate-owned costing snapshot. Existing workforce/materials work is reused. Qualification records and Basic RAMS now have service/UI foundations and focused persistence evidence; provider-backed Service files, installer-pack integration, normal-browser acceptance, scheduling and unsupported catalogue evidence remain explicit dependencies. Commercial RAMS is recorded separately as not started.
7. **Configurator and drawing proof (3)** — can progress after customer-output contracts are stable. Existing B92 proof is reused; broader product families remain evidence-dependent.
8. **Platform boundary, clean provisioning and bounded cleanup (5)** — extracts stable shared boundaries after the first journeys. Clean database/storage support exists; tenant-neutral assets/package manifest remain implementation gaps. Cross-tenant Core ownership and production RBAC are genuine release blockers recorded outside this 73-entry cohort.

## Package gates

Each package is completed as one user journey and shared-service pass: remaining implementation → focused service/API tests → normal-route browser verification including failure/recovery → Roadmap evidence update → coherent checkpoint/push → short user-acceptance checklist. Prior evidence is reused unless the package changes its contract.

The first implementation pass starts with: **start the day → see real overdue/today work → search/open the exact Client, Enquiry, Project, Estimate, Order or follow-up → complete work and set the next action**. It replaces browser-local dashboard inference rather than adding another CRM store.

## Current continuation point

- Package 1 is technically verified and awaits its bounded user checklist plus the authenticated multi-user/history dependencies recorded in the Roadmap.
- Package 2 Email intake/filing and supplier-RFQ preparation/return are technically verified through likely-record link-or-create → working Estimate RFQ draft → exact selected response → destination/revision review → Quote Returned → Manufacturer Import review handoff. User acceptance and external provider operations remain; do not expand into non-Gmail channels or live EF-CL-028 writes.
- Package 3 now has a verified terms review → Download → prepare → failure/retry → immutable issue/release → Follow Up → reviewed withdrawal browser journey, plus service/Portal proof that a newer revision supersedes earlier offers without changing evidence. Continue at the generic B92 document drawing item shared with Package 7, then run the consolidated ordinary-user checklist and separately authorised controlled delivery.
- Package 4 now has one normal-route disposable browser proof from customer change request through canonical Order and reviewed final confirmation. The Portal/Order/supplier-neutral confirmation slice is implementation-ready and awaits its short user checklist; public access, live delivery, e-signature/legal policy and later procurement/delivery milestones remain incomplete. Continue with Package 5 comparison/technical documents while preserving the shared Package 3/7 configured-drawing dependency.
- Package 5 now has service/API proof and a normal-route disposable browser journey for canonical retained-document registration, exact Project/Position applicability, explicit customer-visibility approval, persisted reload and no provider-binary copy. Comparison hydration preserves reviewed `unmapped` evidence. All four entries remain in progress pending the short ordinary-user checklist; the Ty Clai visual/PDF review and older commercial-breakdown recovery remain explicit. Continue with Package 6 installation operations without treating technical proof as user acceptance.
- Package 6 has focused persistence proof for team/Service routing, internal service targets, installer qualification validity/evidence, and immutable Basic RAMS revisions. Its current continuation point is normal-browser installer qualification and Basic RAMS acceptance, followed by canonical provider-backed Service attachments and installer-pack integration. The consolidated supplier estimate/revision composer is part of Package 2: its bounded browser preparation/failure/retry check passes, but the complete incoming-response and reissue browser journey, uncertain-response linking UI and controlled provider delivery remain outstanding.
- Package 2 response-state correction: acknowledgements now persist as review-required and emit an acknowledgement event, never Quote Returned; later acknowledgements preserve received-document state. Replays return the same next action and refuse a conflicting supplier/Estimate relationship. Saved supplier requests now expose a bounded mailbox reply selector and reuse the existing filing picker. The disposable browser run uses production components and HTTP routes with a fixture Gmail adapter: two same-thread messages on separate bounded pages open their own bodies, and the selected acknowledgement persists without Quote Returned. Browser/profile cleanup independently reports zero remaining; the existing API baseline is preserved. Continue at the new selector's document-filing/import handoff through customer reissue, including failed send/retry and multi-supplier resolution. The complete normal-application journey and ordinary-user acceptance remain outstanding.
- Package 2 filing continuation: the browser now proceeds through reviewed filing, an injected post-storage linking failure, clear partial-success feedback, retry with exactly one stored file, persisted revised-document receipt and the exact saved-document import event. The provider adapter writes and verifies disposable bytes; it does not prove real Drive or Manufacturer Import processing. Communications and lifecycle paths now share receipt-state updates so filing cancels pending follow-up and retry repairs incomplete receipt projection. Seven communications relationship tests and the 22-test Portal/procurement suite pass. Next is the full application's extraction/review/reissue journey and multi-supplier aggregate completion, with ordinary-user acceptance still separate.
- Package 2/4 multi-supplier receipt gate: verification and customer preparation now check every non-cancelled, non-superseded supplier request. An acknowledgement is still outstanding; errors name the suppliers requiring documents. Sending a prepared customer email rechecks this state before provider delivery while preserving recovery of already-sent evidence. The disposable Portal/procurement test proves one returned document plus a second acknowledgement blocks preparation, and new supplier work after preparation blocks send without changing the prepared record. Staff verification feedback reports remaining work. This is a receipt gate only: supplier-specific field-check provenance, complete application browser reissue and ordinary-user acceptance remain outstanding.
- Package 2/4 source-bound review: field checks now retain returned source kind, document identity, revision and available checksum as a source identity. Relinking changed evidence clears verification; partial rechecking cannot reuse checks against the earlier source. Previous checks are snapshotted in immutable review history, general checks update without accumulating NULL-position duplicates, and interrupted review saves remain unverified. Disposable lifecycle proof covers replacement, partial/full recheck, history immutability and reissue rejection. This still uses the parent's selected returned document: independent per-supplier field-review sets, a history UI and the complete browser reissue journey remain the next work, not delivered scope.
