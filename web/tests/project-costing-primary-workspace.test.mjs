import test from "node:test";
import assert from "node:assert/strict";
import {readFile} from "node:fs/promises";

test("Project Costing removes permanent commercial scaffolding and contains import",async()=>{const[workspace,worksheet,supplier,css]=await Promise.all([readFile("src/features/estimateCommercial/EstimateCommercialWorkspace.tsx","utf8"),readFile("src/features/projectCalculatorLab/ScenarioCostingWorksheet.tsx","utf8"),readFile("src/features/projectCalculatorLab/SupplierCommercialReview.tsx","utf8"),readFile("src/features/estimateCommercial/estimateCommercialWorkspace.css","utf8")]);assert.match(workspace,/role="dialog"/);assert.match(workspace,/aria-modal="true"/);assert.match(workspace,/setTab\("costing"\)/);assert.doesNotMatch(workspace,/Supplier Quotations &amp; Project Costing \(Preview\)|Temporary development entry|Create disposable development estimate/);assert.ok(worksheet.indexOf('title="Products / Supply Only"')<worksheet.indexOf('title="Site Visit / Travel"'));assert.doesNotMatch(worksheet,/return <><SiteVisitTravelPanel/);assert.match(worksheet,/title="Site Visit \/ Travel"[\s\S]*<SiteVisitTravelPanel/);assert.match(supplier,/<details className="ui-card supplier-commercial-review supplier-commercial-review--compact">/);assert.match(supplier,/Additional project supplier discount/);assert.match(css,/estimate-commercial__modal-scrim/)});

test("canonical Products actions and commercial ownership remain intact",async()=>{const worksheet=await readFile("src/features/projectCalculatorLab/ScenarioCostingWorksheet.tsx","utf8");for(const value of ["Import Manufacturer Quote","Add Position","Configure Position","Edit Configuration","Customer / Client Discount","Products after Client Discount"])assert.match(worksheet,new RegExp(value.replaceAll("/","\\/")));assert.match(worksheet,/siteVisitAllocatedToProducts\?"Included in Products"/)});
